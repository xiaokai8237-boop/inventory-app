# 物流筐收发系统 — 数据同步问题求助文档

日期：2026-08-03
求助目的：请另一位 AI 协助排查"网页端数据上传到云端成功、APP 恢复成功，但店面/记录数据始终无法传到 APP"的问题。

---

## 一、系统架构（必读）

这是一个**单页面应用（单 HTML）+ Cloudflare Pages Functions/KV 云备份**：

- **前端**：`index.html`（单文件，所有逻辑都在里面）
- **数据存储**：
  - 本地用浏览器 `localStorage`，按账号隔离
  - 账号作用域 key：`kuanwei_stores__<手机号>`（店面配置）、`kuanwei_inventory_data__<手机号>`（收发记录）、`kuanwei_goods_names__<手机号>`（筐配置）、`kuanwei_daily_pref`（日报偏好）
  - 未登录时用全局 key（无后缀）：`kuanwei_stores`、`kuanwei_inventory_data` 等
- **云端备份**：`POST /backup` 存到 KV 的 `data_<手机号>`；`GET /backup?account=<手机号>` 读取
- **关键函数**：
  - `backupToCloud()`：点"立即备份到云端"，把本地数据整体上传覆盖云端
  - `restoreLoggedIn(phone)` / `autoSyncFromCloud(phone)` / `doVerifyAccount()`：恢复云端数据到本地
  - `migrateDataToScope()`：登录时把全局 key 数据迁移到账号作用域

## 二、问题现象

用户（手机号 15558088023）在网页端能看到 6 家台州店（且排好序），在网页端点"立即备份到云端"提示成功，在 APP 点"恢复云端数据"也提示成功，**但 6 家店和记录数据始终没有出现在 APP 上**。用户已多次按步骤操作（重新登录、恢复、上传），均无效。

## 三、已排查出的关键事实

1. **云端 `data_15558088023` 的 storeConfig（店面配置）反复被清空**：
   - 我（当前 AI）曾多次向云端写入 6 家店 storeConfig，验证云端确实有数据（`storeConfig` 数组含 6 家台州店）。
   - 但只要用户网页端一"上传"，云端 storeConfig 就**变回空数组**。这证明**用户网页端本地 `kuanwei_stores__15558088023` 上传时是空数组**。

2. **用户网页端本地账号作用域确实没有店面数据**：
   - 用户上传后云端 records（记录）也是空、storeConfig 也是空。
   - 说明用户网页端本地 `kuanwei_stores__15558088023` 和 `kuanwei_inventory_data__15558088023` 都是空的。

3. **但用户说"能看到 6 家店"**：
   - 云端 `uiState.kuanwei_daily_pref`（日报偏好）里有一个 6 家台州店的数组：`["台州海上潮鸣中心店","台州椒江天和路店","台州椒江德智和大厦","台州椒江中山西路店","台州湾吾悦店","台州西站店"]`
   - **怀疑用户看到的"6 家店"来自"日报偏好"（daily_pref），而不是"店面管理"（storeConfig）**。即用户可能从未在店面管理面板真正添加过店面。

4. **三个账号云端 storeConfig 现状**：
   - `15558088023`：我写入后为 6 家台州店（但可能被用户上传清空）
   - `17688560476`：12 家温州店 + 大量记录（完整）
   - `13635685200`：空

5. **已做过的代码修复**（线上已部署到 v6.0.25）：
   - ① 恢复逻辑：云端 storeConfig 为空数组时不再覆盖本地（原来空数组也会覆盖本地，导致本地店面被清空）
   - ② 迁移逻辑：登录时若账号作用域为空且有全局数据，则始终迁移（原来迁移标记已设会跳过）
   - ③ 上传逻辑：本地 storeConfig 为空但云端有店面时，保留云端店面并补回本地（防止空值覆盖云端）

## 四、核心矛盾 / 待确认问题（需要另一位 AI 协助判断）

**核心矛盾**：
> 用户说"网页端能看到 6 家店"，但用户网页端本地的店面配置（storeConfig）上传时是空的。

这 6 家店到底存在哪里？为什么本地 storeConfig 是空却能"看到"？

**请另一位 AI 协助判断的问题**：
1. 用户"看到的 6 家店"是在**「店面管理」面板**里（可编辑/排序），还是只在**日报/记录里选店**的地方出现？（这决定数据存不存在 storeConfig）
2. 用户**网页端**和 **APP** 分别登录的是哪个手机号？（网页端疑似 15558088023，APP 不确定。如果 APP 登录了别的号如 17688560476，恢复的就不是台州 6 家店）
3. 用户网页端的 **localStorage 完整内容**是什么？（需要用户按 F12 → Application → Local Storage 查看，或运行 `JSON.stringify(Object.keys(localStorage))` 导出）
4. 用户是否可能**从未在店面管理添加过店面**，6 家店只是日报偏好的残留？

## 五、可行的下一步方向（供参考）

- **方案 A**：确认用户网页端和 APP 登录账号是否一致；若 APP 登录了别的账号，切换回 15558088023。
- **方案 B**：确认用户"看到的 6 家店"是否真的在店面管理里；如果店面管理是空的，需让用户在店面管理手动添加这 6 家店，或从云端恢复。
- **方案 C**：让用户导出网页端 localStorage 完整数据，确认店面/记录数据实际存在哪个 key（账号作用域 or 全局）。
- **方案 D**：检查用户网页端打开页面时 `autoSyncFromCloud` 是否正常触发并拉取云端数据（需要确认用户网页端当前登录态、是否在线）。

## 六、云端访问方式（供另一位 AI 验证）

- 查云端账号数据：`GET https://inventory-app-9ql.pages.dev/backup?account=15558088023`
- 写云端数据（测试）：`POST https://inventory-app-9ql.pages.dev/backup`，body 含 `{ account, deviceId, data }`
- 管理员接口（可查账号密码，密钥 8023.520）：`POST https://inventory-app-9ql.pages.dev/admin/stats`

---

> 备注：当前 AI 已能确认云端数据写入正常、恢复逻辑代码无 bug（已用自动化测试模拟登录+恢复，6 家店能成功恢复）。问题极可能出在**用户网页端本地数据实际为空**、**账号不一致**、或**"看到 6 家店"其实是日报偏好而非店面配置**这三者之一，需要用户配合确认网页端真实状态。
