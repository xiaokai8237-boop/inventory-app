# 支付接入规划 · 物流筐收发管理系统

> 规划日期：2026-08-11
> 当前状态：测试期（VIP_TEST_MODE=true，所有账号默认 VIP，支付未接入）
> 定位：正式收费上线前的完整方案设计（含前置条件/技术方案/老用户政策/实施步骤）

---

## 一、现状

| 项 | 现状 |
|----|------|
| 定价 | 4 档位已定：月卡 12.8 / 季卡 24.8 / 年卡 58 / 终身卡 128 |
| 会员中心 | 已实现（状态卡 + 4 档位网格 + 权益展示 + 测试期说明） |
| 支付 | **未接入**——点档位弹「支付功能暂未接入，测试期所有账号默认 VIP」 |
| 测试期 | VIP_TEST_MODE=true，isVip() 直接放行，无到期概念 |
| 账号体系 | 手机号验证码登录 + 云端 VIP 到期时间（vipUntil，需求7 已云端双存） |
| 基础设施 | Cloudflare Pages（静态）+ Cloudflare Pages Functions（云函数，无传统服务器） |

---

## 二、前置条件（必须先解决，否则支付接不上）

| # | 条件 | 说明 | 状态 |
|---|------|------|------|
| P-1 | **支付商户号** | 微信支付/支付宝商户号。**个人主体可申请**（微信支付支持个体工商户/个人，需身份证+银行卡；部分渠道需营业执照）。费用 0 元申请，交易费率 0.6%（支付宝个人 0.6%） | ⛔ 未办 |
| P-2 | **备案域名** | 支付回调/下单接口必须用**已备案域名**（与上架 B-5 同一件事）。pages.dev 无法备案 → 需注册备案域名（约 60 元/年 + 备案 1-2 周） | ⛔ 未办 |
| P-3 | **商户证书/密钥** | 微信 API v3 密钥 + 商户私钥证书（申请商户号后自动生成） | ⛔ 未办 |
| P-4 | **对公/个体户收款账户** | 微信/支付宝结算到个人银行卡或个体户对公账户 | ⛔ 未办 |

> **结论：P-1 + P-2 是硬门槛，建议和"上架"一起办（共用备案域名）——一次搞定，避免二次折腾。**

---

## 三、技术方案选型

### 方案对比（基于 Cloudflare Pages Functions 无服务器架构）

| 方案 | 支付方式 | 用户体验 | 技术复杂度 | 适配场景 |
|------|----------|----------|-----------|----------|
| **A. 微信 Native 扫码** | 网页显示二维码，手机微信扫 | 扫码→微信内确认→支付 | 中（无 openid 要求） | **推荐**：手机网页/电脑端都能用，无需公众号 |
| B. 微信 JSAPI | 微信内直接拉起支付 | 最顺滑 | 高（需公众号+openid） | 需公众号，暂不适用 |
| C. 支付宝 H5/扫码 | 支付宝扫码或跳转 | 好 | 中 | 可作补充通道 |
| D. 第三方聚合（虎皮椒/易支付等） | 封装微信/支付宝 | 一般 | 低 | 个体起步可选，费率偏高（1-2%） |

**推荐：A 微信 Native 为主 + C 支付宝扫码为备（两通道并行，用户习惯不同）**。
起步阶段也可先用 D（聚合支付）快速跑通，费率换时间，正式规模化后再切官方。

### 订单流程（以微信 Native 为例）

```
用户选档位(月/季/年/终身)
    ↓
前端 POST /api/pay/create  {phone, planId, deviceId}
    ↓
云函数: 校验登录 + 生成订单号 + 调微信下单API(金额/商品名/notify_url)
    ↓
返回: 二维码URL(code_url) + 订单号
    ↓
前端展示二维码 + 「微信扫一扫支付」
    ↓
支付完成 → 微信回调 POST /api/pay/notify（云函数验签+幂等）
    ↓
云函数: 给该账号 vipUntil += 对应天数（终身=2100年）→ 写 account_ 
    ↓
前端轮询 /api/pay/status {orderNo} → 成功 → 弹「开通成功」→ 刷新会员中心
```

### 关键设计点

| 点 | 设计 |
|----|------|
| 幂等 | 支付回调幂等（同一订单只加一次 VIP，用订单号去重） |
| 防刷 | 下单前校验登录 + 档位金额与商品ID一一对应（防止改价） |
| 回调安全 | 微信验签（API v3 用平台证书验签）+ 回调地址白名单 |
| 订单过期 | 二维码 2 小时有效，超时取消 |
| 会员叠加 | 已 VIP 用户续费 = 在原 vipUntil 基础上加天数（不是重置） |
| 终身卡 | vipUntil 设 2100-12-31（或加 isLifetime 标记） |
| 退款 | 暂不做在线退款（个人主体可先说明"联系管理员人工处理"） |

### 数据存储（沿用现有 KV）

| KV key | 内容 |
|--------|------|
| `pay_order_<orderNo>` | 订单：phone/planId/amount/status(created/paid/closed)/createdAt/paidAt |
| `account_<phone>.vipUntil` | 已存在，续费时叠加 |

### 前端改动点

1. 会员中心「立即开通」→ 调支付（替换现在 toast）
2. 新增支付弹窗/页面：二维码展示 + 轮询状态 + 成功/失败/过期三态
3. VIP_TEST_MODE=false 开关（正式收费时切，7 处拦截自动生效）

---

## 四、老用户政策（测试期 → 正式收费过渡）

**这是最容易得罪人的环节，建议二选一（或组合）：**

| 方案 | 内容 | 优缺点 |
|------|------|--------|
| **方案 1：老用户赠期** | 测试期注册的用户，正式收费后**自动送 1 个月 VIP**（按注册时间） | 好口碑；成本低（1 个月） |
| **方案 2：老用户感恩价** | 首月 0.1 元 / 年卡 5 折（限测试期老账号） | 有收入又有诚意 |
| **方案 3：邀请抵扣** | 正式收费时，之前邀请成功的每个好友 = 减 5 元（已有邀请记录数据） | 拉新和收费联动，最有增长价值 |

> 我的建议：**方案 1 + 3 组合**——老用户送 1 个月 + 邀请记录折现，既安抚老用户又激励拉新。

---

## 五、实施步骤（预计 3-5 天，含等待期）

| 步骤 | 内容 | 依赖 | 时长 |
|------|------|------|------|
| 1 | 注册备案域名 + 提交备案（与上架共用） | — | 1-2 周（等待） |
| 2 | 申请微信支付商户号（个体/个人）+ 支付宝商户 | P-1 | 3-7 天（审核） |
| 3 | 域名解析到 Cloudflare + 迁移部署 | 步骤 1 | 半天 |
| 4 | 后端：支付云函数（下单/回调/查询 + 验签 + 幂等） | 步骤 2 的密钥 | 1-2 天 |
| 5 | 前端：会员中心接入 + 支付弹窗 + 三态 | 步骤 4 | 1 天 |
| 6 | 测试：真机支付全流程（微信/支付宝各一单） | 步骤 4-5 | 半天 |
| 7 | VIP_TEST_MODE=true → false 切换 + 老用户政策生效 | 全部 | 0.5 天 |
| 8 | 上线监控（支付成功率/回调失败率） | — | 持续 |

---

## 六、风险与对策

| 风险 | 对策 |
|------|------|
| 个人主体微信商户申请被拒 | 用个体工商户执照（全家司机可注册个体户，约 0 元）；或先用聚合支付过渡 |
| 备案等待期长 | 域名备案提交后可先跑通支付沙箱，备案下来立即切换 |
| 支付回调丢单 | 前端轮询兜底 + 每日对账脚本（拉取微信账单比对订单状态） |
| 用户支付后 VIP 没到账 | 回调幂等 + 前端「我已支付未到账」申诉入口（管理员手动补发） |

---

## 七、结论

- **技术方案已可行**（Cloudflare Functions 支持微信/支付宝签名与回调，无需传统服务器）
- **最大瓶颈不是技术，是资质**：备案域名 + 商户号（约 2-3 周流程）
- **建议**：现在就可以**启动备案域名 + 商户号申请**（与上架 P0 一起办），等资质下来我 3-5 天完成支付接入
- **过渡期**：保持测试期免费跑通拉新，正式收费用「老用户送 1 月 + 邀请折现」平滑过渡
