# 会话交接简报 — 物流筐收发管理系统（最新版）

> 更新：2026-08-01 | 用途：多设备无缝继续开发（每次开工/收尾自动更新本简报）

## 〇、更新记录

- **2026-08-01**：确认工作流程约定（开工先 git pull + 看本简报，收尾 git add/commit/push）。本地代码与 origin/main 一致。新增 `.gitignore`（忽略 .codebuddy/，防敏感信息泄露）。无代码改动。
- **2026-08-02**：解决 GitHub push 长期失败问题（本机网络连 GitHub 不稳）。① 通过 v2rayN 代理（本地 127.0.0.1:10809 HTTP / 10808 SOCKS）打通 git push；② 额外用 Cloudflare API Token + wrangler 直传部署成功（正式域名 https://inventory-app-9ql.pages.dev 正常 200）。本地与 origin/main 已同步。删除"五、明天 To-Do"待办小节。`.gitignore` 新增忽略 `.wrangler/`（wrangler 缓存目录）。
- **2026-08-02（第二次）**：应用 6 项更新并部署。① 登录功能（改动1/4，本已存在于代码，未重复）② 密码框自动纠错（改动2，autocapitalize/autocorrect/spellcheck）③ 密码框眼睛按钮（改动3，togglePwd + pwd-wrap CSS）④ 明文密码存储 + 管理员用户列表（改动5，auth/admin 后端存 password，前端 setup/forgot 传 password，admin 面板渲染手机号+密码）⑤ 管理员搜索（改动6，adminSearch + renderAdminUsers）。已 push 至 GitHub（commit 1fc6d1d）并 wrangler 部署至 Cloudflare。⚠️ 注意：明文密码存储是用户明确要求的，有安全风险（管理员密钥 8023.520 可见所有用户明文密码），后续建议评估。
- **2026-08-02（第三次）**：管理员入口改造。① 入口触发从"❓ 连点 10 次"改为"❓ 连点 5 次"；② 新增管理员密码验证弹窗（adminAuthModal + doAdminAuth），密码正确（8023.520）后才进入管理员面板；③ 面板内所有操作（统计/搜索/重置密码）不再二次输入密码（adminKey 存变量）。已 push 并部署。
- **2026-08-02（第四次）**：店面排序功能。① 店面对象新增 `sort` 字段（默认=数组下标，从小到大排）；② 店面管理面板每张卡片加"↑/↓"上下移按钮（moveStore 交换 sort）；③ 所有店面显示界面（店面管理、发出/回收 chips、记录/汇总/导出/库存下拉）按 sort 排序；④ 用 `getSortedStores()` 排序视图渲染，原始数组顺序不变（storeIdx 稳定），历史记录/汇总/库存数据不串位。已 push 并部署。
- **2026-08-02（第五次）**：修复 PWA/桌面快捷方式下"相册选/拍照"点击无反应。根因：file input 用 `display:none` + JS `.click()` 触发，在 PWA/iOS Safari/安卓 WebView 下被浏览器安全机制拦截。修复：改为 `<label for="fileInput">` 包裹按钮（原生触发，不依赖 .click()），input 视觉隐藏改用 `position:absolute;opacity:0`。涉及店面拍照（storeOcrFile/storeOcrAlbum）和拍照识别（photoInput/photoAlbumInput）。已 push 并部署。
- **2026-08-02（第六次）**：修复 PWA 仍用旧缓存导致修复不生效。sw.js 缓存版本号 v2→v3（activate 自动清旧缓存）；SW 注册加 `updateViaCache:'none'` 并监听 updatefound 提示"有新版本请重开"。
- **2026-08-02（第七次）**：安卓用户卸载 PWA 后重装引导。已安装过再卸载，浏览器不再触发 beforeinstallprompt（deferredPrompt 为 null），点"添加到主屏幕"会显示引导。新增 `installAndroidTip` 安卓专属引导（浏览器菜单→添加到主屏幕/安装应用），JS 按 UA 区分显示安卓/苹果引导。sw 缓存 v6。
- **2026-08-02（第八次）**：修复店面上下移按钮无效 bug。根因：`moveStore` 里 `cfg.map((s,i)=>({...s, sort:i}))` 用数组下标强制覆盖 sort，导致交换的 sort 值被重置、排序永不生效。修复：交换 sort 后按 sort 值重排得到显示序列，再据此给每个店重赋 0..n-1 的 sort（数组物理顺序不变，storeIdx 稳定，不依赖店名）。已用 node 验证 A 下移→B,A,C 正确。sw 缓存 v7。
- **2026-08-02（第九次）**：店面排序改为"长按拖动"。移除 ↑/↓ 上下移按钮，卡片加 `draggable`（HTML5 drag API）+ 触屏长按拖动 polyfill（touchstart 长按400ms进入，touchmove 高亮目标卡，touchend 完成 dragStoreDrop）。dragStoreDrop 按显示顺序重排 sort（数组物理顺序不变，storeIdx 稳定）。触屏(pointer:coarse)自动启用 touch 拖动。sw 缓存 v8。
- **2026-08-02（第十次）**：店面卡片恢复原尺寸。去掉 sc-head 里新增的"长按拖动排序"提示文字，sc-idx 恢复数字序号；修复拖动样式新增时拆散的 `.store-card` box-shadow 孤立 CSS 块。拖动功能保留。sw 缓存 v9。
- **2026-08-02（第十一次）**：新版本更新改为"确认弹窗+强制重启"。检测到新 SW（updatefound）弹 versionConfirmModal 确认框；用户点"立即更新"→ 发 SKIP_WAITING 让新 SW 立即接管 + 600ms 后 location.reload() 强制重启加载新版本。sw.js 加 message 监听 SKIP_WAITING。sw 缓存 v10。
- **2026-08-02（第十二次）**：店面排序从"长按拖动"改回"按键"。卡片去掉 draggable 和全部拖拽事件/触屏 polyfill；恢复 ▲▼ 上下移按钮（.sc-move-col 竖排，放在 sc-head 序号右侧，30x22 小按钮，首/末自动禁用），不增加卡片高度。moveStore 排序逻辑保留。sw 缓存 v11。
- **2026-08-02（第十三次）**：优化 ▲▼ 按钮位置和大小（按钮叠在序号圆里的问题）：sc-move-col 加 margin-left:6px 间距，按钮加大到 28x26、字号13px、白底更清晰，仍不增卡片高度。sw 缓存 v12。
- **2026-08-02（第十四次）**：删除键显示不全。把删除键从 sc-head（一行挤不下）移到卡片底部新增 .sc-foot（右对齐、虚线分隔、弱化红色），头部只留 序号+▲▼+店名。sw 缓存 v13。
- **2026-08-02（第十五次）**：主页角落加小版本号 v2.0.0（homeWizard 右上角，10px 小字半透明）。sw 缓存 v14。
- **2026-08-02（第十六次）**：安卓桌面 PWA 下相册选点击无反应（file input 被 WebView 拦截，网页正常）。改为 `openAlbumPicker`：优先用 `window.showOpenFilePicker()`（安卓 PWA 可用），失败/不支持 fallback 原生 input.click()。涉及拍照识别（photoAlbumInput）和店面拍照（storeOcrAlbumInput）。sw 缓存 v15。
- **2026-08-02（第十七次）**：用户用小米手机。triggerAlbumInput 增加动态创建全屏透明 file input 触发（方案2），适配小米 WebView 需可见 input 才响应；选完自动清理临时 input。sw 缓存 v16。
- **2026-08-02（第十八次）**：日报生成加预览确认（方案B）。新增 dailyPreviewModal 预览确认弹窗（textarea 可编辑）；点"生成日报"先弹预览（可修改文字），点"确认生成"后才把内容设为最终日报并显示复制按钮。sw 缓存 v17。
- **2026-08-02（第十九次）**：日报生成界面加实时预览窗口。generateDailyReport(true) 支持仅计算返回 text；新增 dailyLivePreview 实时预览区（生成按钮上方），线路/门店/月份/信息项勾选变化时 updateDailyLivePreview() 自动刷新。sw 缓存 v18。
- **2026-08-02（第二十次）**：安卓 PWA 拍照/相册选不生效，终极方案：相册选改用**原生可见 file input 直接作为按钮**（.native-file-btn，伪元素::before 显示"🖼️ 相册选"，不隐藏、不用 label/JS 触发），用户直接点击原生 input 走系统选择器，安卓 WebView 最可靠。拍照仍用 label+capture。涉及 photoAlbumInput、storeOcrAlbumInput。sw 缓存 v19。
- **2026-08-02（第二十一次）**：已登录账号免密恢复/备份。① auth/verify 支持 `skipPwd`：当 deviceId 匹配账号 lastDeviceId 时免密返回数据；登录/注册时记录 lastDeviceId（doLogin/doVerifyAccount/doSetupAccount 传 deviceId）。② 已登录用户点"恢复数据"（openVerifyModal）直接免密恢复（restoreLoggedIn），不再弹密码框。③ backupToCloud（立即备份）本就不需密码，确认无改动。sw 缓存 v20。
- **2026-08-02（第二十二次）**：全量操作/偏好云端同步（换设备无陌生感）。backupToCloud payload data 升 version 2，新增 `uiState`（collectUiState：日报偏好、教程已看、删除确认设置、OCR密钥、近2日相册照片）。恢复路径（restoreLoggedIn/autoSyncFromCloud/doVerifyAccount/doLogin）应用 uiState（applyUiState）。核心数据（记录/店面/筐）本就自动同步，登录账号时全部写入 account_ 手机号。sw 缓存 v21。
- **2026-08-02（第二十三次）**：无操作定时兜底备份。新增 startPeriodicBackup()：每30分钟 setInterval 自动 backupToCloud(false)（仅已登录时有账号才备份，后台静默零干扰），页面启动时调用。配合原有"操作后3秒防抖备份"，确保数据不丢。sw 缓存 v22。
- **2026-08-02（第二十四次）**：操作后备份延迟 3秒 → 1秒（scheduleCloudBackup setTimeout 3000→1000）。sw 缓存 v23。
- **2026-08-02（第二十五次）**：记录页新增"额外发收登记"（针对所有门店）。① 主界面加"➕ 额外发收登记"按钮 → 弹窗（goods-input-list 样式，每筐一行"额外发出/额外回收"输入框，含表头）。② 单个"💾 保存登记"按钮：发出列→qtyIn 记录、回收列→qtyOut 记录，storeIdx=-1、storeName='额外发收'、extra:true。③ 统计纳入：今日概览自动计入；日报/汇总/历史/导出在单店筛选时也保留额外记录；库存追踪排除额外（无店面归属）。sw 缓存 v24。
- **2026-08-02（第二十六次）**：一次上线 4 大功能（commit fbf7ddd，sw v25）。① **深色模式**：`:root` 增加 `html[data-theme="dark"]` 变量覆盖，header/tabs/卡片/输入框改 CSS 变量；顶部加 🌙/☀️ 切换按钮（toggleTheme），偏好存 `kuanwei_theme`（加入 UI_KEYS 云端同步），未设置时跟随系统；JS 内联硬编码浅色底用 CSS 覆盖层兼容。② **今日状态卡片**：首页向导新增卡片（renderTodayStatus），显示今日发出/回收总数、已录入店面数/总店面数、录入进度条、各店已录✓/待录○ chips、未录入店面提醒；随 renderTodayOverview/backToHomeWizard 刷新。③ **多账号本地数据隔离复核加强**：数据层引入账号作用域（scopeKey 返回 `key__<手机号>`），登录账号后记录/筐/店面配置写入该账号作用域，实现本地彻底隔离、切换账号不串号；登录/设密时 migrateDataToScope() 把旧全局数据迁入账号作用域并清空全局；logout 先备份云端再退出；首页新增"数据账号"徽标（renderAccountBadge）。④ **离线完整可用**：sw.js 预缓存 xlsx CDN 库、fetch 移除同源限制缓存外部资源，保证离线对账/导出可用；前端加离线提示条（offlineBanner）+ online/offline 监听（initOffline），离线存本机、联网自动后台备份。缓存版本 v24→v25。
- **2026-08-02（第二十七次）**：布局调整（commit 673afb2，sw v26）。① "额外发收登记"按钮从主页面移到**记录页**（manualForm 快捷录入卡片内，语音/拍照按钮下方，全宽橙色按钮）。② "今日状态"卡片从主页面顶部移到**主页面下方**（选择操作卡片之后）。用户明确：未选择的功能以后不再提及。
- **2026-08-02（第二十八次）**：全面代码检查+修复（commit db536dd，sw v27）。**修复BUG**：① logout 清 `kuanwei_phone` 作用域，退出后不再写错账号隔离空间（高危）；② 离线横幅 `display:''`→`block`，离线提示现在能显示；③ saveExtraRecord 用录入页日期而非强制今天；④ 今日概览/汇总/日报统计 `+= r.qtyIn` 补 `|| 0` 防 NaN；⑤ 今日概览 storeMap 初始化移出循环优化；⑥ 深色模式补类名浅色块覆盖（库存表头/备份步骤/别名校验/导入区/对账差异行/别名chip）。**精简冗余**：loadGoodsConfig/loadStoreConfig 抽公共 `loadNamedConfig`；saveGoodsNamesStorage 并入 saveGoodsConfig；新增 `refreshRecordViews()` 统一5处记录页刷新；renderTodayStatus 两 forEach 合并；cleanStoreName 噪音清除改一次正则。JS 语法校验通过。
- **2026-08-02（第二十九次）**：登录系统改造（commit 026025a，sw v28）。① **首页角落登录胶囊**：header 左侧，未登录"🔑登录"/已登录"👤155****8023"（中间4位星号脱敏 maskPhone）。② **账号操作菜单**：点击胶囊弹出（切换用户/退出登录/注销账号）。③ **切换用户强制密码**：走 loginModal 的 /auth/verify，绝无免密。④ **禁用免密恢复**：openVerifyModal 一律要求密码，restoreLoggedIn 不再自动触发。⑤ **账号注销**：两步确认（警告"永久删除"+输密码二次确认）+ 后悔药按钮（角落小字，点击告知数据未真删可联系管理员）。⑥ **软删除+临时代码**：注销后释放手机号、数据冻结为临时代码 tmp_<code>，管理员可恢复。⑦ **后端接口**：新增 `/auth/delete`（软注销）、`/admin/restore-account`（临时代码→新手机号）、`/admin/delete-temp`（彻底删除），admin/stats 增加 pendingDelete 列表。⑧ **管理员恢复界面**：adminModal 新增"已注销待恢复"区块，显示临时代码+原手机号，可恢复/彻底删除。⑨ **未登录锁定**：页面顶部橙色提醒横条 loginNotice + 录入/保存控件禁用（locked-input），保存函数加登录校验双保险。**功能缺入口排查**：所有功能均有入口，唯一后端 delete-account 无前端入口（已用注销+临时代码机制替代）。语法校验全过。
- **2026-08-02（第三十/三十一次）**：登录入口位置优化（commit b6ac36e + d048b1c，sw v29→v31）。① 登录胶囊从 header 移到**首页顶部**（原"数据账号"徽标位置，全宽居中按钮）；header 恢复干净不再被占用。② **更改密码**整合进登录胶囊菜单（已登录：切换用户/更改密码/退出登录/注销账号）。③ **数据备份页优化**：移除退出登录/更改密码按钮（已移胶囊菜单），accountStatus 精简为账号状态+恢复数据/登录/忘记密码+操作指引；删除"使用教程再次查看"冗余按钮；账号显示改脱敏格式。
- **2026-08-02（第三十二次）**：数据备份页删除账号卡片（commit 06563f3，sw v32），只保留"恢复云端数据"按钮。
- **2026-08-02（第三十三次）**：修复小米PWA桌面拍照/相册选不可用（commit 305b43f，sw v33）。**根因**：label 包裹 hidden input 委托 + native-file-btn 伪元素方案在小米PWA(WebView)下失效。**改法**：拍照/相册选按钮改为普通 button + onclick 动态创建**原生可见** file input（移出可视区但不隐藏）并立即 .click()，触发后移除；拍照 capture="environment"，相册不带 capture；同时适配店面拍照（triggerPhoto('camera'/'album','store')）。删除对旧 photoInput/photoAlbumInput/storeOcrAlbumInput 的引用（closeOcrResultModal 里的 value 重置）。
- **2026-08-02（第三十四次及之后）**：登录系统多轮优化 + 密码系统改造（commit 305b43f→467d704，sw v33→v40，版本 4.3.1）。包括：测试期临时取消登录锁定、版本号规则与APP_VERSION集中管理、**密码改明文存储与校验**（前端登录/改密/注销/恢复/重置全部传明文，后端verify/setup/reset/delete改明文比对并兼容旧哈希）、5200账号(13635685200)密码多次排查与重置。**注**：后端 `admin/reset` 现支持明文（newPasswordHash 可选）；`handleSetup` 不再强制 passwordHash；`handleVerify` 优先明文比对、旧哈希兜底。当前云端所有账号明文密码字段为空（旧版只存哈希），需重新设置/重置密码才会写入明文。
- **2026-08-02（第三十五次）**：密码明文**全路径生产接口实测**（commit 467d704，版本 4.3.1）。用测试号完整跑通：①设置账号 `/auth/setup` ✅ ②登录 `/auth/verify` 明文 ✅ ③改密码（verify旧密+setup新密）✅新密能登/旧密401 ④忘记密码 `/auth/reset` 密保+新密码 ✅ ⑤管理员重置 `/admin/reset` 明文 ✅ ⑥注销 `/auth/delete` 明文校验+临时代码 ✅ ⑦管理员恢复 `/admin/restore-account` 临时代码→新手机号 ✅ ⑧彻底删除 `/admin/delete-temp` ✅。测试数据已清理，不影响真实账号。
- **2026-08-02（第三十六次）**：强制更新弹窗（commit fc27ba6，版本 4.3.2，sw v41）。更新弹窗改为**强制更新不可跳过**：去掉"稍后"按钮，只有"立即更新"，点遮罩不关闭，显示新版本号。目的：每次部署后强制用户加载最新版，保证顺利使用。当前 5200 账号(13635685200)密码=`Abc@123456`（明文已写入，登录验证通过）。
- **2026-08-02（第三十七次）**：**重大根因修复**（commit 1d7a01b，版本 5.0.0，sw v44）。**核心BUG**：`account_<手机号>` KV key 被 `/auth/setup`（账号认证+密码）和 `/backup`（纯业务数据，无密码字段）**共用覆盖**，导致登录后云备份把密码字段冲成空 → 改密"密码错误"、明文反复变空。**修复**：业务数据改存独立 `data_<account>` key；auth/verify、admin/restore 同步适配。**账号状态**：15558088023、13635685200 均已重置为 `12345678` 并验证登录成功。
- **2026-08-02（第三十八次）**：**全面代码审查+修复**（commit 7eb5562，版本 6.0.0，sw v46）。**修复**：① doOCR/doStoreOCR 未配密钥时全屏遮罩永久卡死（补 hideLoading）；② 注销冻结丢 uiState + data_ 缺失不回退残留业务（补全+回退）；③ migrateDataToScope 未清空全局 key 导致多账号隔离失效串号（改移动语义）；④ logout 未刷新数据视图（补 refreshRecordViews）；⑤ 切换账号云端无数据时界面不刷新（登录后无条件刷新）；⑥ kuanwei_last_save 未写入导致旧云数据覆盖新本地（保存时写时间戳）；⑦ 删除 hashPassword 死代码。**审查确认**：所有弹窗关闭路径完整、面板切换对称、前后端字段一致。**遗留安全**（测试期暂不加固，上线前需处理）：adminKey 硬编码前端、`/backup?account` 无鉴权可读、明文密码日志。
- **2026-08-02（第三十九次）**：修复语音录入动态加载效果被遮挡（commit 8f6d820，版本 6.0.1，sw v47）。**根因**：语音识别浮层 `#voiceOverlay` 与语音录入弹窗 `.store-ocr-modal` 的 z-index 同为 9998，且浮层在 DOM 中更靠前，录音时浮层被弹窗盖住，动态加载（脉冲麦克风）显示不全。**修复**：`.voice-overlay` 的 z-index 提升到 10000（最顶层），录音动态效果不被遮挡。sw v46→v47。
- **2026-08-02（第四十次）**：全面排查并修复弹窗层级遮挡同类问题（commit c199b58，版本 6.0.2，sw v48）。用子代理全量审查所有 z-index 与 DOM 顺序。**修复**：① `#loadingOverlay` 加载浮层 z-index 9998→10000（与语音浮层统一，从任何弹窗触发 OCR 都不被遮挡）；② `voiceConfirmModal`（语音识别确认）、`dailyPreviewModal`（日报预览）、`versionConfirmModal`（强制更新）三个"从父弹窗内打开/随时异步弹出"的子弹窗单独提升 z-index 至 10001，确保不被父弹窗/浮层盖住（尤其 versionConfirmModal 是强制更新不可关，若被盖会死锁）；③ `parseVoiceText` 打开语音确认框时立即收起 voiceOverlay（不再等 onend 的500ms延迟），避免浮层残留。**审查确认无需处理**：photoPanelModal 是死代码（从未调用）、OCR loading 与结果弹窗时序正确、account-menu 与 modal 同帧移除、gd-modal 删除类互斥。sw v47→v48。
- **2026-08-02（第四十一次）**：优化语音识别错误处理（commit f0a0a4d，版本 6.0.3，sw v49）。用户截图反馈小米PWA下语音录入弹"识别出错: not-allowed"。**根因**：浏览器/PWA未授权麦克风权限，SpeechRecognition.start() 拒绝启动。**修复**：① `startVoice` 启动前先 `navigator.mediaDevices.getUserMedia({audio:true})` 主动请求权限（部分PWA/WebView需此步才能弹权限框），获取后立即释放 stream（仅为触发权限）；② `onerror` 错误映射为中文友好提示（not-allowed/audio-capture/no-speech/network/aborted 等分别给出明确引导）；③ 权限类错误用醒目 toast 提示"请在浏览器/PWA设置中允许麦克风权限"；④ 错误时立即收起浮层（不等1.5s延迟），让用户可立即重试或改用手动输入；⑤ start() 用 try/catch 包裹，捕获 InvalidStateError 等同步异常。sw v48→v49。
- **2026-08-02（第四十二次）**：保存失效加固（commit f627d4d + 3d1af35，版本 6.0.4，sw v50）。用户反馈"保存按键失效"。**排查**：用 playwright 真机打开线上页验证——登录状态下保存按钮 `pointerEvents:auto`、`locked:false`，代码无 bug；保存失效最可能是"未登录导致按钮被 locked-input 锁定（pointer-events:none 点击无反应）"或"店面为空被拦截提示"。**加固**：`refreshRecordViews()` 内新增 `updateLoginLock()`，每次刷新视图强制同步登录锁定状态，根治"已登录但按钮残留锁定"的边界场景。**附带清理**：误提交的测试临时文件（.playwright-cli/、_check.js）已从 git 移除，并在 .gitignore 追加忽略规则。sw v49→v50。
- **2026-08-02（第四十三次）**：修复店面管理保存无反应（commit 9f40ee7，版本 6.0.5，sw v51）。用户明确"店面管理页面有改动但点保存没反应"。**根因**：`saveStorePanelChanges()` 内 `const cfg = loadStoreConfig()` 声明为 const，但函数底部 `cfg = cfg.map(...)`（sort 归一化）**对 const 变量重赋值** → 运行时抛 `TypeError: Assignment to constant variable`，函数在第3609行中断，导致 `saveStoreConfig`/`renderStorePanelUI`/`showToast` 都不执行 → 保存完全无反应。**修复**：`const cfg` 改为 `let cfg`。**验证**：playwright 真机在 https 域设置店面数据后调用 `saveStorePanelChanges()`，返回"调用成功，未报错"。sw v50→v51。
- **2026-08-02（第四十四次）**：修复店面排序保存丢失（commit 03c2192，版本 6.0.6，sw v52）。用户明确"排序后点保存提示成功但排序没保存"。**根因**：`saveStorePanelChanges()` 底部 `cfg = cfg.map((s, idx) => ({ ...s, sort: idx }))` **用数组物理下标覆盖 sort**；而排序机制是交换 sort 字段（数组物理顺序不变，保 storeIdx 稳定），导致用户排的序在保存时被物理下标重置丢失。**修复**：改为按现有 sort 值归一化（`sort` 排序→按显示顺序重赋 0..n-1），保留用户排序，数组物理顺序不变。**验证**：node 模拟"B移到A前→保存"，保存后显示顺序仍为 B,A，sort 正确。sw v51→v52。
- **2026-08-02（第四十五次）**：修复店面排序"刷新后又恢复"（commit 4a6ca85，版本 6.0.8，sw v54）。用户明确"保存的店面顺序又恢复了"。**根因**：`autoSyncFromCloud()`（每次刷新/登录自动调用）**无条件用云端 storeConfig 覆盖本地**；若用户保存排序后立即刷新（`scheduleCloudBackup` 有1秒防抖，云端可能还是旧排序）或备份未完成，刷新时拉到的云端旧 storeConfig 覆盖本地新排序 → 顺序恢复。**修复**：① `saveStoreConfig`/`saveGoodsConfig` 保存时写 `kuanwei_last_save` 时间戳（原仅 `saveDataArr` 写）；② `autoSyncFromCloud` 覆盖 storeConfig/goodsConfig 前做新旧判断——本地最后保存时间晚于云端 backupTime 则保留本地（不覆盖）；③ 时间戳读取移到 `saveDataArr` 之前（避免其刷新本地时间戳导致判断失效）。**注意**：此修复需用户**重新排序一次**（触发 saveStoreConfig 写新时间戳）后刷新才不恢复；若云端已是用户最新排序则无影响。sw v53→v54。
- **2026-08-02（第四十六次）**：检查并修复同类错误（commit 4b618d5，版本 6.0.9，sw v55）。用户要求"检查同类型错误并修复"，用子代理全面排查两类。**修复**：① **const 重赋值（与已修店面保存同类）**——`removeStoreFromPanel`（删除店面）`const cfg` 被 `cfg=cfg.map(...)` 重赋值 → TypeError 删除无反应；改 `let cfg` 并按 sort 语义归一化（保留剩余店相对排序）。② **云同步旧数据覆盖本地**——`autoSyncFromCloud` 原对 records 用"云端优先"合并、且无条件 `applyUiState` 覆盖本地偏好（theme/daily_pref/ocr_keys 等）；改为整体套 `cloudNewer` 判断——仅云端比本地新才合并 records/覆盖配置/偏好，本地新则全部保留，彻底杜绝"刷新后数据回退"。**审查确认无需处理**：`doVerifyAccount`/`restoreFromCloud` 是用户主动"恢复数据"语义（以云端为准，合理）、`restoreLoggedIn` 是 dead code（无调用）、其余 40+ 处 `.map/.filter` 重赋值均为 `let` 安全。sw v54→v55。
- **2026-08-02（第四十七次）**：修复 PWA 桌面快捷方式拍照/相册选无效（commit 20cf39b，版本 6.0.10，sw v56）。用户明确"手机桌面快捷方式进入拍照相册选无效，是不是权限问题"。**根因**：`triggerPhoto()` 动态创建的 file input 用 `left:-9999px` **移出可视区**——PWA/standalone WebView 下浏览器因 input 不可见而**阻止唤起系统文件选择器**（拍照/相册都无效）；且拍照需相机权限。**修复**：① `triggerPhoto` 的 input 从"移出可视区"改为**全屏透明可见覆盖**（`position:fixed;top:0;left:0;width/height:100%;opacity:0`，与已验证可靠的 `triggerAlbumInput` 一致）；② 拍照前主动 `navigator.mediaDevices.getUserMedia({video})` 请求相机权限（获取后立即释放）。**结论**：既有权限因素（相机需授权），也有实现因素（input 不可见被阻止唤起）；两者均已处理。所有触发点（拍照识别弹窗 2015/2016、店面拍照 1168/1169）统一走 `triggerPhoto`，已全部覆盖。sw v55→v56。

## 一、核心结论（当前状态）

**项目已完整交付，可正常使用。** 从 v1 迭代到最新版，全功能上线：

| 项 | 状态 |
|----|------|
| 程序 | ✅ 完整可用（单 HTML 文件 3840 行） |
| 部署 | ✅ Cloudflare Pages 固定网址（不再用临时隧道） |
| 云端 | ✅ KV 自动备份 + 手机号密码账号体系 + 管理员重置 |
| 产物 | ✅ 演示视频（67秒 mp4）+ 功能演示 PPT（11页） |
| 质检 | ✅ 最高规格 19 项全过（语法/结构/安全/接口全通） |

## 二、资源位置速查

| 资源 | 位置/链接 |
|------|-----------|
| **线上网址（永久固定）** | https://inventory-app-9ql.pages.dev/ |
| **代码仓库（GitHub）** | https://github.com/xiaokai8237-boop/inventory-app |
| **演示视频下载** | https://inventory-app-9ql.pages.dev/demo.mp4 |
| **演示PPT下载** | https://inventory-app-9ql.pages.dev/demo.pptx |
| **主程序** | `/workspace/index.html` |
| **云端接口** | `/workspace/repo/functions/`（backup.js / proxy.js / auth/[[path]].js / admin/[[path]].js） |
| **演示单据图** | `/workspace/演示单据.png` |
| **录屏脚本** | `/workspace/demo_record.py` |
| **PPT生成脚本** | `/workspace/gen_ppt.js` |
| **本地代理脚本** | `/workspace/proxy_server.py`（如需本地跑OCR） |

## 三、账号体系（重要，勿泄露）

| 项 | 值 |
|----|-----|
| 管理员密钥 | `8023.520` |
| 管理员入口 | 使用说明页最后一条"每天要录很多店怎么办？"前 ❓ 连点 5 次 → 输入管理员密码（8023.520）→ 进入面板 |
| 临时重置密码 | `12345678` |
| 密码规则 | ≥8位，含字母+数字+符号 |
| 手机号规则 | 11 位，1 开头 |
| 安全机制 | 云端只存哈希、输错 5 次锁 10 分钟、密保问题找回 |

## 四、功能清单（当前版）

- ✅ 店面管理（增删改+别名+拍照批量导入+保存按钮）
- ✅ 5种筐类型管理（增删改+别名）
- ✅ 录入三方式：手动 / 语音（Web Speech）/ 拍照OCR（百度）
- ✅ 拍照识别：路单自动识别物流箱，可编辑/新增行/列偏移
- ✅ 历史记录（筛选/删除/来源标记）
- ✅ 汇总（日期+店面维度，单店汇总）
- ✅ 对账（导出Excel/导入对比差异）
- ✅ 库存追踪（发出/回收/待收）
- ✅ 数据备份与恢复（导出/导入 .json）
- ✅ 云备份（Cloudflare KV 自动备份）
- ✅ 账号密码体系（设置/恢复/改密/忘记密码/密保找回）
- ✅ 管理员重置（隐藏入口）
- ✅ 傻瓜教程（保姆级使用说明 + 备份教程弹窗）
- ✅ 加载动画/自适应/emoji/新UI

## 五、电脑上如何继续开发

```bash
# 1. 克隆仓库
git clone https://github.com/xiaokai8237-boop/inventory-app.git

# 2. 改完推送（Cloudflare Pages 自动部署，1-2分钟生效）
git add -A && git commit -m "说明" && git push

# 3. 本地预览（纯界面，OCR需云端）
python3 -m http.server 8080  # 打开 http://localhost:8080/
```

> 部署说明：Cloudflare Pages 项目已绑定 KV（变量名 `BACKUP_KV`），推送即自动部署，无需手动。测试账号数据已清理/测试用。

## 六、遗留决策口子（你拍板）

1. **做不做模板化改造**（通用化后卖价翻倍，受众从"物流配送"扩到"任意进出库管理"）？
2. **卖源码 还是 SaaS 化**？决定下一步开发方向。

## 七、注意事项

- 数据每台手机独立（localStorage）+ 云端账号备份（手机号+密码）
- 清缓存后：输入手机号+密码从云端恢复
- 云端接口免费额度：Cloudflare Pages 免费、KV 免费（写入10万/天）、百度OCR默认密钥已预置（可能限流，卖出去需换独立key）
- 管理员入口是隐藏的，勿写进公开教程
