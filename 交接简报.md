# 会话交接简报 — 物流筐收发管理系统（最新版）

> 更新：2026-08-03 | 线上 v6.0.44 sw v90 version.txt=26 | 用途：多设备无缝继续开发（每次开工/收尾自动更新本简报）

## 〇、更新记录

- **2026-08-03（第七十五次）**：**UI 全面升级-配色清理+材质升级+动效升级+氛围细节**，线上 v6.0.43 sw v89 version.txt=25（commit 83f5db0）。**用户要求**：在已升级的"深空蓝晶"主题基础上"全部优化"（配色、材质、动效、氛围），且排版不动；不满意可回退。**重要前置**：已先备份 v6.0.42 原版到 `backup_20260803_pre-v6.0.42/`（index.html/sw.js/version.txt 全套，已 gitignore 防止入库），保留7天供回退。**实现（全部仅改颜色/视觉/动效，零排版改动）**：①**配色统一**：扫描所有硬编码旧色（#f0f7ff/#f0fdf4/#fff3cd/#f8fafc/#3b82f6/#10b981 等约 50+ 处）全部替换为 CSS 变量——新增 `--blue-soft/--green-soft/--amber-soft/--red-soft`（浅/深色双套）、`--glass-bg/--glass-border/--focus-ring`；提炼出 `{additional}` 4 处新增辅助色，OCR 高置信/警告/对账行/账号注销/管理员面板/欢迎弹窗/使用说明提示色块/账号状态块/云备份状态块/删除确认/编辑记录/合计行等行内色全部统一。②**材质升级**：弹窗（`.store-ocr-content`/`.gd-content`）改玻璃拟态（`backdrop-filter:blur(18px) saturate(160%)` + var(--glass-bg) + 发光边框）。③**动效升级**：页面切换淡入（.page.active 加 pageFadeIn 0.25s）；今日状态数字弹入（.ter-num/.tsb-num/.stat-val 加 numPop 0.45s cubic-bezier 弹跳）；按钮按压反馈强化（.btn:active 加 box-shadow 收缩+亮度降低）；body 加 0.3s 背景色过渡，主题切换平滑。④**氛围细节**：深色模式 body 加 10 个极淡星点 radial-gradient（星空质感）；::-webkit-scrollbar 美化（深浅两套，6px 窄条半透明）；输入框聚焦环统一 var(--focus-ring)。⑤**首屏元素**：offline-banner/login-notice 配色统一到 var(--amber-soft)/var(--warning)。⑥**深色模式补丁精简**：旧补丁 `div[style*="#f0f7ff"]` 等引用旧色字符串的规则已自动失效（行内色全改成 var），保留无害但冗余，未来清理时可批量删除。⑦**版本与备份**：APP_VERSION 6.0.42→6.0.43、sw v88→v89、version.txt 24→25；`.gitignore` 新增 `backup_*/` 忽略。**已用 playwright 截图验证**（浅色+深色双模式）：无 JS 错误；首页大按钮/标题渐变/今日状态卡片/欢迎弹窗玻璃效果/数字显示均正常；深色星空点可见。**排版完全未动**；**回退路径**：`cp backup_20260803_pre-v6.0.42/* .` 即可恢复原版。注：仓库 working tree 还有 android/splash.png 等上次未提交的改动（与本次无关），未在本次 commit 中带，避免混淆。
- **2026-08-03（第七十五次）**：**店序按筐种独立**（commit 39a62a7），线上 v6.0.44 sw v90 version.txt=26。**用户需求**：点发出→选筐类型→进入的页面，"调整店序"在该筐种页面设置的顺序只适用于该筐种；每种筐（鲜食/面包/低温/冷冻/常温）各自一套独立店序。**实现**（改动最小，零调用点变动）：①声明区 `let emitOrder = null` → `let emitOrderCache = {}`（字典缓存）；EMIT_ORDER_KEY **保持不变**（`kuanwei_emit_store_order`，以 kuanwei_ 前缀自动纳入云备份全量快照）。②存储结构升级为 `{v:2, orders:{筐名:[店序]}}`，旧版全局数组自动迁移为所有筐种默认序（一次性落盘，零数据丢失）。③getEmitOrder/saveEmitOrder 函数加可选 `goodsName` 参数，未传时内部按 `emitGoodsIdx` 取当前筐种——所有调用点（renderEmitStoreList/renderEmitStoreOrder/emitDrop/emitMoveOrder/renderEmitVoiceConfirm/applyEmitVoice）**一行都不用改**。④自动同步：新店追加到所有筐种顺序末尾；删除店面自动从所有筐种顺序中移除。⑤云备份 `collectUiState` 包含自动收集所有 kuanwei_ 前缀 key 的逻辑，**新结构自动纳入全量快照，换设备后每筐独立序原样还原**，备份代码零改动。**验证**：playwright 跑了 10 个场景（迁移/独立/新店追加/删店/UI 渲染顺序）全部通过，无 JS 错误；UI 截图确认鲜食筐独立序生效。**用户要求**：重新设计所有界面颜色/配色/主题，要稍微有科技感、高级感；不能动现有排版。**实现**（全部仅改颜色，零排版改动）：①` :root` 变量全面升级——浅色：主色 `#3b82f6`→`#3d5afe`（靛蓝科技蓝）、背景 `#f0f5fa`→`#eef2fb`（淡蓝调）、强调色 `#10b981`→`#00b894`（科技青）、新增 `--primary-glow/--success-glow` 发光变量、阴影升级带 `--shadow-glow`、边框加透明度科技感。②深色：背景 `#0f172a`→`#0a0f1e`（深邃星空黑）、卡片 `#1e293b`→`#131a2e`（深蓝黑）、主色 `#60a5fa`→`#7c9bff`（亮靛蓝）、强调色 `#34d399`→`#2fe6bf`（霓虹青）。③body 加径向渐变光晕（蓝+青双光斑，浅深色各自一套）。④header 加 backdrop-filter 玻璃模糊 + 底部科技感边框。⑤标题 h1 用渐变文字（主色→强调色）。⑥主按钮 btn-primary/danger/success/warning 全加渐变+发光阴影。⑦语音按钮渐变统一到新变量+发光。⑧首页大按钮 home-btn-issue 升级三段蓝紫渐变+大发光，home-btn-recover 升级青绿渐变+发光。⑨store-card 加 hover 上浮+发光阴影交互，sc-name 输入框聚焦加发光圈。⑩关键硬编码灰背景统一为 `var(--soft-bg)`（#f8fafc/#f8f9fa/#fafafa 等）、表格表头、导入区、OCR 结果区、track-tbl 等。⑪启动页 bootOverlay 升级三色渐变（深蓝→亮蓝→深紫）。⑫theme-color meta 升级主色。**已用 playwright 真机截图验证**：浅色+深色双模式正常渲染，无 JS 错误；首页大按钮/标题渐变字/今日状态卡片/使用说明卡片配色协调科技感足。**排版完全未动**。
- **2026-08-03（第七十三次）**：**今日状态卡片改为按5种筐显示发出数**，线上 v6.0.41 sw v87 version.txt=23（commit 570ba00）。**用户要求**：今日统计卡片只改「今日发出/今日回收」这两块，替换成 5 种筐各自的发出总数；卡片其他地方不动、可拉长、5行间距紧凑、不要合计行。**实现**：`renderTodayStatus()` 里 `today-status-sum` 的「今日发出」「今日回收」两个数字框改为 `today-emit-list` 紧凑列表——固定显示5种筐（🍱鲜食/🍞面包/🧊低温/❄️冷冻/📦常温）各今日发出数（qtyIn），无数据显示0；「已录店面」数字框保留原位；进度条/店名chips/待录入提醒完全不动；新增 `.today-emit-list/.today-emit-row/.ter-name/.ter-num` CSS（行距紧凑）。**已验证**（playwright 本地）：未登录/无数据时5种筐全显0不崩；注入今日记录后各筐发出数正确（鲜食12/面包6/常温10，低温冷冻0），已录店面保留，进度条正常。
- **2026-08-01**：确认工作流程约定（开工先 git pull + 看本简报，收尾 git add/commit/push）。本地代码与 origin/main 一致。新增 `.gitignore`（忽略 .codebuddy/，防敏感信息泄露）。无代码改动。
- **2026-08-03（第七十二次）**：**店面管理提示文案同步**，线上 v6.0.40 sw v86 version.txt=22（commit 36c89f6）。用户截图反映"我的店面(0)"——已确认 v6.0.39 修复后 openStorePanel 不再自动导入/恢复（0家店正确）。但页面提示文案仍写"若店面为空会从日报偏好自动导入"与实际不符，改为"可通过下方批量导入用拍照/相册快速添加"。
- **2026-08-03（第七十一次）**：**鲜食筐图标改盒饭 + 店面管理不再自动恢复**，线上 v6.0.39 sw v85 version.txt=21（commit 169a9c2）。①鲜食筐图标从🥬改为🍱(盒饭样式)；②`openStorePanel` 打开店面管理时**不再自动调用 importStoresFromDailyPref**（用户要求彻底去掉自动恢复备份），店面数据完全由用户手动维护，打开店面管理不再自动填充/改动。
- **2026-08-03（第七十次）**：**发出页优化**，线上 v6.0.38 sw v84 version.txt=20（commit ba41186）。**用户要求**：①5种筐图标各不相同②切换筐按钮变短、筐名字号大突出③保存成功弹提示框（回主页/切换筐两个按钮）。**实现**：①新增 `goodsIcon(name)` 筐→图标映射（鲜食🥬/面包🍞/低温🧊/冷冻❄️/常温📦/回收♻️），筐选择弹窗和顶部显示对应图标；②发出页顶部切换按钮改文字"切换"变短、筐名 22px 字重800 突出；③保存成功弹 `emitSaveResultModal`（显示"保存成功"+已存X家共Y个「筐名」），两个按钮：🏠回到主页(closeEmitPanel)、🔁切换筐(保留发出页弹筐选择)。**已验证**：5种筐图标去重后各不相同；顶部筐名22px/800/图标正确；保存弹窗两个按钮；点切换筐发出页保持打开；点回主页关闭。
- **2026-08-03（第六十九次）**：**发出页增加拍照识别**，线上 v6.0.37 sw v83 version.txt=19（commit c076367）。`emitTriggerPhoto()` 动态唤起拍照/相册 → `emitDoOcr()` 用腾讯云 OCR 识别路单文字 → 提取数字序列 → 弹确认框(可改) → 按店序填入（与语音一致）。复用现有 compressImage + /tcloud 接口。
- **2026-08-03（第六十八次）**：**发出页面重构（新设计上线）**，线上 v6.0.36 sw v82 version.txt=18（commit b6a5065）。**用户需求**：点「发出」→ 先弹筐类型选择 → 所有店面+每店一个输入框录所选筐的发出量 → 语音按店顺序填数+确认框 → 调整店序(仅发出页) → 保存返回主页。**实现**：①首页「发出」改走 `openEmitFlow()`(先选筐 emitGoodsSelectModal)；②新增 `emitPanel`(所有店面按发出页独立店序+每店输入框)；③店序弹窗(拖动+▲▼，存 kuanwei_emit_store_order 独立 key，仅影响发出页，随云快照备份)；④语音录入 emitStartVoice(只提取数字序列→确认框可改→按店顺序覆盖填入)；⑤保存 saveEmitRecords(按店生成 qtyIn 记录→返回主页)；⑥回收页 manualForm 保持原样。**已验证**：选筐→发出页3店输入→保存生成记录+返回主页；店序调整[店C,店A,店B]；语音"3 6 2"确认后填[3,6,2]；今日统计正确识别发出量；回收页正常；未登录不崩。**拍照识别**：已实现（第六十九次 emitDoOcr 腾讯云 OCR 提取数字按店序填）。**启动页**：确认当前 splash_1080x1882.png 已是用户准备的图（项目/android/desktop 三处哈希一致），无需替换。
- **2026-08-03（第六十七次）**：**备份升级为"完整快照"（云端=最后一次完整操作状态）**，线上 v6.0.35 sw v81 version.txt=17（commit 7c6058f）。**用户核心诉求**：每次最新上传要包含当前页面/APP 的全部数据——业务数据（发出/回收/汇总/额外数量、店面、筐）+ 软件状态（深色模式、日报勾选项、操作习惯等），云端=用户最后一次操作时的完整快照。**现状**：backupToCloud 已上传 records/goodsConfig/storeConfig/uiState(含daily_pref/theme等)。**本次增强**：`collectUiState()` 增加**动态收集所有其他 kuanwei_ 用户状态 key**（排除账号隔离/登录态/设备/时间戳/迁移标记/照片），任何偏好/操作习惯（含未来新增）都会自动打包备份，真正做到"所有所有都要记录"。applyUiState 自动还原这些 key。**用户确认"云端数据=用户最后一次操作"**。
- **2026-08-03（第六十六次）**：**自动上传改为3秒，整包覆盖云端**，线上 v6.0.34 sw v80 version.txt=16（commit 317587c）。**用户要求**"云端自动上传改为3秒，每一次上传，上传成功之后，覆盖掉上一次上传数据"。**实现**：`scheduleCloudBackup()` 防抖从 1000ms 改为 3000ms（保存后3秒自动备份）。**覆盖机制**：backup 本就是 POST /backup 整包覆盖 `data_<account>`，每次上传成功后云端为最新一次数据（不累加），已满足"覆盖上一次"要求。服务器端空值保护（records/storeConfig 本地空但云端有则保留）仍生效，防误清空。
- **2026-08-03（第六十五次）**：**去掉自动恢复/同步，恢复改为手动点击**，线上 v6.0.33 sw v79 version.txt=15（commit 33d84cd）。**用户要求**"去掉自动恢复备份功能，恢复备份需要用户自己手动点击"。**实现**：移除所有 `autoSyncFromCloud` 自动调用——①页面打开时自动同步（原6506）②登录成功后（doVerifyAccount）③设置密码后（doSetupAccount，改为仅 backupToCloud 首次备份）④忘记密码重置后（doForgotReset）。函数定义保留（避免引用报错），恢复/同步完全靠用户手动点「恢复云端数据」。登录/重置后提示"如需要云端数据，请手动点恢复云端数据"。
- **2026-08-03（第六十四次）**：**修复"店面顺序+别称被自动同步覆盖丢失"（真正根因）**，线上 v6.0.32 sw v78 version.txt=14（commit 6980cd6）。**问题**：用户在网页端把吾悦店移到第一位+添加别称43-5/5店并保存（但没点立即备份），网页版打开时 `autoSyncFromCloud` 自动同步，cloudNewer=true → **用云端旧店面配置覆盖本地刚设置的顺序/别称**，导致顺序变回默认、别称丢失。**根因**：autoSyncFromCloud 在 cloudNewer=true 时无条件用云端 storeConfig 覆盖本地；云端 backupTime 因其他数据（如记录）较新，但 storeConfig 是旧的（用户设置未上传）。**修复**：autoSync 里 storeConfig 改为**仅当本地为空时才从云端拉取**；本地已有店面配置（用户主动设置）时保留本地。**已验证**（最严格场景 cloudNewer=true）：本地湾吾悦排第一+别称43-5/5店 → autoSync 后**完整保留**（第一个=湾吾悦、别称43-5/5店）。
- **2026-08-03（第六十三次）**：**备份后弹窗展示备份详情**，线上 v6.0.31 sw v77 version.txt=13（commit a828e55）。用户要求"点击立即备份后显示备份了哪些东西，包括店名、店序、店名的别称、数据"。**实现**：新增 `backupResultModal` 弹窗 + `showBackupResult()`——手动备份成功后展示：店面总数/筐数/记录数，以及**按 sort 排序的店面列表（顺序编号 + 店名 + 别称）**。**已验证**：备份后弹窗显示店B(B分店)/店A/店C(C店别名)，顺序+别称正确。**同时恢复用户数据**：用户截图证明店面+别称"43-5"同步正常，问题仅在于用户未点"立即备份"把本地排序/别称传云端。
- **2026-08-03（第六十一次）**：**服务器端终极保护：云端不再被空 records/storeConfig 覆盖**（commit 2ec91fb，改 functions/backup.js）。**问题**：即使前端 v6.0.29 加了 records 空值保护，用户设备若仍是旧版本（sw 缓存未更新），上传空 records 仍会覆盖云端，导致用户 APP 反复恢复不到数据（云端 records 多次被清空）。**根治**：`handleBackupPost` 上传时，若本地 records 为空数组但云端已有记录 → 保留云端记录；storeConfig 同理。**客户端无论新旧版本都无法再冲掉云端数据**。**已验证**：模拟旧版设备上传空 records → 云端保留 3 条记录 + 6家店。**云端现完整**：3条记录（鲜食5/面包8888/低温5555）+ 5类筐 + 6家店（含sort）+ daily_pref。**用户 APP 重新恢复即可看到**。
- **2026-08-03（第六十次）**：**修复"记录被空值覆盖云端"（APP恢复无数据根因）**，线上 v6.0.29 sw v75 version.txt=11（commit c5cc90a）。**问题**：用户 APP 点恢复提示"已恢复"但无数据——查云端 records 被清空（22:03 上传把本地空 records 覆盖云端，用户之前录入的低温筐5555等3条记录丢失；storeConfig 6家店仍在）。**根因**：`backupToCloud` 只有 storeConfig 空值保护，**records 无保护**——本地 records 空时上传会覆盖云端有记录（清缓存/换设备场景）。**修复**：backupToCloud 增加 records 空值保护——本地 records 空且云端有记录时保留云端记录并补回本地。**数据补救**：用之前云端看到的3条记录（鲜食筐5/面包筐8888/低温筐5555，台州海上潮鸣中心店 8/3）重新写入云端，云端现 records=3条+storeConfig=6家店+daily_pref 完整。**用户 APP 重新点恢复即可看到**。
- **2026-08-03（第五十九次）**：**修复导出/导入缺店面**，线上 v6.0.28 sw v74 version.txt=10（commit cfe2af1）。**问题（另一AI诊断）**：`exportData()` 只导出 records+goodsNames，**不含 storeConfig（店面）**；`importData()` 也只导入记录+筐，导致用"导出/导入"迁移数据时店面（含sort顺序）丢失。**修复**：① exportData 补上 `goodsConfig`（完整含别名）、`storeConfig`（含 sort）、`uiState`；② importData 导入 storeConfig/goodsConfig/uiState，旧格式导出无 storeConfig 时从记录店名补店面，导入后刷新店面界面（renderStorePanelUI/initAllStores）。**已验证**：导出 JSON 含 storeConfig(6家含sort)+goodsConfig+uiState+records。**记录同步**：已真实验证用户录入"低温筐5555"后云端立即有记录，APP恢复能拿到记录+顺序，同步链路正常。
- **2026-08-03（第五十八次）**：**实施另一AI的预防措施，防止数据源分裂**，线上 v6.0.27 sw v73 version.txt=9（commit a4d90a6）。核心原则：**店面唯一数据源 = storeConfig（店面管理）**。**实现**：① 新增 `syncDailyPrefWithStores()`：日报打开时用当前 storeConfig 校验并刷新 `kuanwei_daily_pref.stores`（只保留存在的店、新店自动加入），使日报偏好始终是 storeConfig 镜像；② `backupToCloud` 备份前健康检查：storeConfig 为空但日报偏好有店 → 自动导入，避免空店面覆盖云端；③ restoreLoggedIn/doVerifyAccount/autoSyncFromCloud 恢复/同步后校验：storeConfig 空则从日报偏好自动导入；④ 店面管理面板加提示"店面请在此维护，日报/记录/汇总自动同步"。**已验证**：日报偏好残留已删店名→打开日报自动清除并加入新店；storeConfig空+备份→自动导入日报店。**补充**：第57次已实现 openStorePanel 打开店面管理时若 storeConfig 空则自动从日报偏好导入（importStoresFromDailyPref）。
- **2026-08-03（第五十七次）**：**按另一AI建议根治"店面从未添加导致上传丢失"**，线上 v6.0.26 sw v72 version.txt=8（commit b107d24）。**确认根因（另一AI验证云端一致）**：用户网页端 storeConfig 本来就是空的，6 家台州店只存在于 `kuanwei_daily_pref`（日报偏好），用户从未在店面管理添加过店面。**实现**：新增 `importStoresFromDailyPref()`——店面管理为空时自动从 `kuanwei_daily_pref.stores` 导入店面到 storeConfig；在 `openStorePanel()` 打开店面管理时若 storeConfig 为空则自动导入并 toast。**已验证**：模拟 storeConfig空+daily_pref有6家→打开店面管理→自动导入6家店(storeCount=6)，且 saveStoreConfig 触发自动备份上传云端(云端 storeConfig 有6家)。**用户操作**：更新到 v6.0.26 后，打开网页端店面管理→自动出现6家店→点备份→APP恢复即有店面。
- **2026-08-03（第五十六次）**：**根治"店面数据反复丢失"**，线上 v6.0.25 sw v71 version.txt=7（commit 87ccfbf）。**问题**：用户网页端本地 storeConfig 为空（用户看到的6家店来自 daily_pref 日报偏好，非店面配置），每次上传都用空 storeConfig 覆盖云端6家店，导致云端反复被清空、APP恢复不到。**修复**：`backupToCloud` 上传时若本地 storeConfig 为空数组且云端已有店面，则**保留云端店面**并**补回本地**（saveStoreConfig），避免空值覆盖云端。**已验证**：本地店面空+点上传→本地自动得到6家店，云端保持6家不被清空。云端 `data_15558088023` 已写入6家店 storeConfig+daily_pref（用 curl 可靠写入；playwright fetch 写入不稳定，返回ok但云端不更新，原因未明）。**用户操作**：更新到最新版后，在网页端点"恢复云端数据"（或直接点上传，会自动把云端6家店补回本地），再 APP 点"恢复云端数据"。若用户网页端账号作用域确实一直为空，可能用户从未在店面管理真正添加过店面（6家店仅在日报偏好）。**注意**：用户网页端登录账号疑似15558088023（上传写入了 data_15558088023）。
- **2026-08-03（第五十五次）**：修复"网页上传→APP恢复，任何数据（店面/记录）都传不过来"的真正根因，线上 v6.0.24 sw v70 version.txt=6（commit bb91533）。**真正根因**（playwright 复现）：用户网页端店面/记录存在**全局 key**（`kuanwei_stores`/`kuanwei_inventory_data`），而非账号作用域；未登录时 `loadStoreConfig()` 回退读全局→能看到6家店；但 `kuanwei_migrated__15558088023` 标记已设→登录时 `migrateDataToScope()` 被"已迁移跳过"挡掉→全局数据永不迁入账号作用域→登录上传读账号作用域(空)→云端 storeConfig 空→APP恢复拿不到。**修复**：`migrateDataToScope()` 移除"迁移标记已设就跳过"逻辑，改为每次登录检查"账号作用域为空且全局有数据才迁入"（作用域已有数据不覆盖，安全幂等）。**已验证**：全局有6家+标记已设+登录→修复后迁移成功。云端 `data_15558088023` 重新写入6家店 storeConfig+daily_pref。**用户需做**：网页端退出登录再重新登录（触发迁移），再点"立即备份到云端"；APP 点"恢复云端数据"。仅改网页代码，未改壳，不发安装包。
- **2026-08-03（第五十四次）**：修复"店面数据同步不过来（APP 恢复不到店面）"，线上 v6.0.23 sw v69 version.txt=5（commit 74b97ed）。**用户确认网页端能看到 6 家台州店并排好序**。**根因定位**：云端 `data_15558088023` 的 storeConfig 是空 `[]`，而 `autoSyncFromCloud`/`restoreLoggedIn`/`doVerifyAccount`/`restoreFromCloud` 四处都有 `if (cloud.storeConfig && Array.isArray(...)) saveStoreConfig(...)`，空数组也满足条件 → **用云端空 storeConfig 覆盖本地已配置的 6 家店**，再备份空值回云端，形成双向清空。已用 playwright 复现：本地注入 6 家店 + 已登录 + 加载页面，autoSync 用云端 `[]` 覆盖后本地变 0 家。**修复**：四处统一改为 `if (cloud.storeConfig && Array.isArray(cloud.storeConfig) && cloud.storeConfig.length > 0)`，云端空店面不再覆盖本地。**数据补救**：通过 playwright 用本地修复版 index.html 把 6 家台州店 storeConfig + 完整 uiState（daily_pref 6家店）备份到云端，云端 `data_15558088023` 现已有 6 家店 storeConfig + goodsConfig(5类) + daily_pref。**注意**：诊断过程中曾用干净浏览器登录 15558088023 触发备份，覆盖过一次云端 uiState 丢失 daily_pref，已用 playwright 重新写回完整 uiState（含 daily_pref）修复。仅改网页代码，**未改壳，不发安装包**。
- **2026-08-03（第五十三次）**：启动页修复 + Capacitor SplashScreen 插件（shell 已构建 APK，代码改动在 android/ 与 capacitor.config.json）。用户反馈启动页全白/比例不对/闪白。**根因**：Capacitor 默认用 AndroidX `Theme.SplashScreen`（只认 `windowSplashScreenBackground`/`AnimatedIcon`，不认旧 `windowBackground`），且 webView 加载远程页面时初始白屏。**修复**：① 安装 `@capacitor/splash-screen` 插件；② `capacitor.config.json` 配 `plugins.SplashScreen`（launchShowDuration 2000、androidScaleType CENTER_CROP 不变形、splashFullScreen、showSpinner false）；③ styles.xml 用 `Theme.SplashScreen` + `windowSplashScreenAnimatedIcon` 设为透明（避免 Android 12 双图标冲突），由插件全屏显示启动图并保持到网页加载完成（不闪白）；④ 启动图用桌面 `splash_1080x1882.png` 放 `drawable/splash.png`。**产物**：`物流筐-debug测试版2.apk`（5.3MB，需重装生效）。
- **2026-08-03（第五十二次）**：Capacitor APK 打包 + 安卓拍照/云同步修复（线上 v6.0.15 sw v61）。详见上文。**注意**：用户要求"发出页面重新设计"但只给设计方案、未执行（已 revert，线上回到 v6.0.15）；"以后 APP 版本号同步递增"；"只需更新网页的改动不发安装包，只有改壳才发"；"更新提示框可选非强制"。
- **2026-08-02**：解决 GitHub push 长期失败问题（本机网络连 GitHub 不稳）。① 通过 v2rayN 代理（本地 127.0.0.1:10809 HTTP / 10808 SOCKS）打通 git push；② 额外用 Cloudflare API Token + wrangler 直传部署成功（正式域名 https://inventory-app-9ql.pages.dev 正常 200）。本地与 origin/main 已同步。删除"五、明天 To-Do"待办小节。`.gitignore` 新增忽略 `.wrangler/`（wrangler 缓存目录）。
- **2026-08-02（第二次）**：应用 6 项更新并部署。① 登录功能（改动1/4，本已存在于代码，未重复）② 密码框自动纠错（改动2，autocapitalize/autocorrect/spellcheck）③ 密码框眼睛按钮（改动3，togglePwd + pwd-wrap CSS）④ 明文密码存储 + 管理员用户列表（改动5，auth/admin 后端存 password，前端 setup/forgot 传 password，admin 面板渲染手机号+密码）⑤ 管理员搜索（改动6，adminSearch + renderAdminUsers）。已 push 至 GitHub（commit 1fc6d1d）并 wrangler 部署至 Cloudflare。⚠️ 注意：明文密码存储是用户明确要求的，有安全风险（管理员密钥 8023.520 可见所有用户明文密码），后续建议评估。
- **2026-08-02（第三次）**：管理员入口改造。① 入口触发从"❓ 连点 10 次"改为"❓ 连点 5 次"；② 新增管理员密码验证弹窗（adminAuthModal + doAdminAuth），密码正确（8023.520）后才进入管理员面板；③ 面板内所有操作（统计/搜索/重置密码）不再二次输入密码（adminKey 存变量）。已 push 并部署。
- **2026-08-02（第四次）**：店面排序功能。① 店面对象新增 `sort` 字段（默认=数组下标，从小到大排）；② 店面管理面板每张卡片加"↑/↓"上下移按钮（moveStore 交换 sort）；③ 所有店面显示界面（店面管理、发出/回收 chips、记录/汇总/导出/库存下拉）按 sort 排序；④ 用 `getSortedStores()` 排序视图渲染，原始数组顺序不变（storeIdx 稳定），历史记录/汇总/库存数据不串位。已 push 并部署。
- **2026-08-02（第五次）**：修复 PWA/桌面快捷方式下"相册选/拍照"点击无反应。根因：file input 用 `display:none` + JS `.click()` 触发，在 PWA/iOS Safari/安卓 WebView 下被浏览器安全机制拦截。修复：改为 `<label for="fileInput">` 包裹按钮（原生触发，不依赖 .click()），input 视觉隐藏改用 `position:absolute;opacity:0`。涉及店面拍照（storeOcrFile/storeOcrAlbum）和拍照识别（photoInput/photoAlbumInput）。已 push 并部署。
- **2026-08-02（第六次）**：修复 PWA 仍用旧缓存导致修复不生效。sw.js 缓存版本号 v2→v3（activate 自动清旧缓存）；SW 注册加 `updateViaCache:'none'` 并监听 updatefound 提示"有新版本请重开"。
- **2026-08-02（第七次）**：安卓用户卸载 PWA 后重装引导。已安装过再卸载，浏览器不再触发 beforeinstallprompt（deferredPrompt 为 null），点"添加到主屏幕"会显示引导。新增 `installAndroidTip` 安卓专属引导（浏览器菜单→添加到主屏幕/安装应用），JS 按 UA 区分显示安卓/苹果引导。sw 缓存 v6。
- **2026-08-02（第八次）**：修复店面上下移按钮无效 bug。根因：`moveStore` 里 `cfg.map((s,i)=>({...s, sort:i}))` 用数组下标强制覆盖 sort，导致交换的 sort 值被重置、排序永不生效。修复：交换 sort 后按 sort 值重排得到显示序列，再据此给每个店重赋 0..n-1 的 sort（数组物理顺序不变，storeIdx 稳定，不依赖店名）。已用 node 验证 A 下移→B,A,C 正确。sw 缓存 v7。
- **2026-08-02（第九次）**：店面排序改为"长按拖动"。移除 ↑/↓ 上下移按钮，卡片加 `draggable`（HTML5 drag API）+ 触屏长按拖动 polyfill（touchstart 长按400ms进入，touchmove 高亮目标卡，touchend 完成 dragStoreDrop）。dragStoreDrop 按显示顺序重排 sort（数组物理顺序不变，storeIdx 稳定）。触屏(pointer:coarse)自动启用 touch 拖动。sw 缓存 v8。
- **2026-08-02（第十次）**：店面卡片恢复原尺寸。去掉 sc-head 里新增的"长按拖动排序"提示文字，sc-idx 恢复数字序号；修复拖动样式新增时拆散的 `.store-card` box-shadow 孤立 CSS 块。拖动功能保留。sw 缓存 v9。
- **2026-08-02（第十一次）**：新版本更新改为"确认弹窗+强制重启"。检测到新 SW（updatefound）弹 versionConfirmModal 确认框；用户点"立即更新"→ 发 SKIP_WAITING 让新 SW 立即接管 + 600ms 后 location.reload() 强制重启加载新版本。sw.js 加 message 监听 SKIP_WAITING。sw 缓存 v10。
- **2026-08-02（第十二次）**：店面排序从"长按拖动"改回"按键"。卡片去掉 draggable 和全部拖拽事件/触屏 polyfill；恢复 ▲▼ 上下移按钮（.sc-move-col 竖排，放在 sc-head 序号右侧，30x22 小按钮，首/末自动禁用），不增加卡片高度。moveStore 排序逻辑保留。sw 缓存 v11。
- **2026-08-02（第十三次）**：优化 ▲▼ 按钮位置和大小（按钮叠在序号圆里的问题）：sc-move-col 加 margin-left:6px 间距，按钮加大到 28x26、字号13px、白底更清晰，仍不增卡片高度。sw 缓存 v12。
- **2026-08-02（第十四次）**：删除键显示不全。把删除键从 sc-head（一行挤不下）移到卡片底部新增 .sc-foot（右对齐、虚线分隔、弱化红色），头部只留 序号+▲▼+店名。sw 缓存 v13。
- **2026-08-02（第十五次）**：主页角落加小版本号 v2.0.0（homeWizard 右上角，10px 小字半透明）。sw 缓存 v14。
- **2026-08-02（第十六次）**：安卓桌面 PWA 下相册选点击无反应（file input 被 WebView 拦截，网页正常）。改为 `openAlbumPicker`：优先用 `window.showOpenFilePicker()`（安卓 PWA 可用），失败/不支持 fallback 原生 input.click()。涉及拍照识别（photoAlbumInput）和店面拍照（storeOcrAlbumInput）。sw 缓存 v15。
- **2026-08-02（第十七次）**：用户用小米手机。triggerAlbumInput 增加动态创建全屏透明 file input 触发（方案2），适配小米 WebView 需可见 input 才响应；选完自动清理临时 input。sw 缓存 v16。
- **2026-08-02（第十八次）**：日报生成加预览确认（方案B）。新增 dailyPreviewModal 预览确认弹窗（textarea 可编辑）；点"生成日报"先弹预览（可修改文字），点"确认生成"后才把内容设为最终日报并显示复制按钮。sw 缓存 v17。
- **2026-08-02（第十九次）**：日报生成界面加实时预览窗口。generateDailyReport(true) 支持仅计算返回 text；新增 dailyLivePreview 实时预览区（生成按钮上方），线路/门店/月份/信息项勾选变化时 updateDailyLivePreview() 自动刷新。sw 缓存 v18。
- **2026-08-02（第二十次）**：安卓 PWA 拍照/相册选不生效，终极方案：相册选改用**原生可见 file input 直接作为按钮**（.native-file-btn，伪元素::before 显示"🖼️ 相册选"，不隐藏、不用 label/JS 触发），用户直接点击原生 input 走系统选择器，安卓 WebView 最可靠。拍照仍用 label+capture。涉及 photoAlbumInput、storeOcrAlbumInput。sw 缓存 v19。
- **2026-08-02（第二十一次）**：已登录账号免密恢复/备份。① auth/verify 支持 `skipPwd`：当 deviceId 匹配账号 lastDeviceId 时免密返回数据；登录/注册时记录 lastDeviceId（doLogin/doVerifyAccount/doSetupAccount 传 deviceId）。② 已登录用户点"恢复数据"（openVerifyModal）直接免密恢复（restoreLoggedIn），不再弹密码框。③ backupToCloud（立即备份）本就不需密码，确认无改动。sw 缓存 v20。
- **2026-08-02（第二十二次）**：全量操作/偏好云端同步（换设备无陌生感）。backupToCloud payload data 升 version 2，新增 `uiState`（collectUiState：日报偏好、教程已看、删除确认设置、OCR密钥、近2日相册照片）。恢复路径（restoreLoggedIn/autoSyncFromCloud/doVerifyAccount/doLogin）应用 uiState（applyUiState）。核心数据（记录/店面/筐）本就自动同步，登录账号时全部写入 account_ 手机号。sw 缓存 v21。
- **2026-08-02（第二十三次）**：无操作定时兜底备份。新增 startPeriodicBackup()：每30分钟 setInterval 自动 backupToCloud(false)（仅已登录时有账号才备份，后台静默零干扰），页面启动时调用。配合原有"操作后3秒防抖备份"，确保数据不丢。sw 缓存 v22。
- **2026-08-02（第二十四次）**：操作后备份延迟 3秒 → 1秒（scheduleCloudBackup setTimeout 3000→1000）。sw 缓存 v23。
- **2026-08-02（第二十五次）**：记录页新增"额外发收登记"（针对所有门店）。① 主界面加"➕ 额外发收登记"按钮 → 弹窗（goods-input-list 样式，每筐一行"额外发出/额外回收"输入框，含表头）。② 单个"💾 保存登记"按钮：发出列→qtyIn 记录、回收列→qtyOut 记录，storeIdx=-1、storeName='额外发收'、extra:true。③ 统计纳入：今日概览自动计入；日报/汇总/历史/导出在单店筛选时也保留额外记录；库存追踪排除额外（无店面归属）。sw 缓存 v24。
- **2026-08-02（第二十六次）**：一次上线 4 大功能（commit fbf7ddd，sw v25）。① **深色模式**：`:root` 增加 `html[data-theme="dark"]` 变量覆盖，header/tabs/卡片/输入框改 CSS 变量；顶部加 🌙/☀️ 切换按钮（toggleTheme），偏好存 `kuanwei_theme`（加入 UI_KEYS 云端同步），未设置时跟随系统；JS 内联硬编码浅色底用 CSS 覆盖层兼容。② **今日状态卡片**：首页向导新增卡片（renderTodayStatus），显示今日发出/回收总数、已录入店面数/总店面数、录入进度条、各店已录✓/待录○ chips、未录入店面提醒；随 renderTodayOverview/backToHomeWizard 刷新。③ **多账号本地数据隔离复核加强**：数据层引入账号作用域（scopeKey 返回 `key__<手机号>`），登录账号后记录/筐/店面配置写入该账号作用域，实现本地彻底隔离、切换账号不串号；登录/设密时 migrateDataToScope() 把旧全局数据迁入账号作用域并清空全局；logout 先备份云端再退出；首页新增"数据账号"徽标（renderAccountBadge）。④ **离线完整可用**：sw.js 预缓存 xlsx CDN 库、fetch 移除同源限制缓存外部资源，保证离线对账/导出可用；前端加离线提示条（offlineBanner）+ online/offline 监听（initOffline），离线存本机、联网自动后台备份。缓存版本 v24→v25。
- **2026-08-02（第二十七次）**：布局调整（commit 673afb2，sw v26）。① "额外发收登记"按钮从主页面移到**记录页**（manualForm 快捷录入卡片内，语音/拍照按钮下方，全宽橙色按钮）。② "今日状态"卡片从主页面顶部移到**主页面下方**（选择操作卡片之后）。用户明确：未选择的功能以后不再提及。
- **2026-08-02（第二十八次）**：全面代码检查+修复（commit db536dd，sw v27）。**修复BUG**：① logout 清 `kuanwei_phone` 作用域，退出后不再写错账号隔离空间（高危）；② 离线横幅 `display:''`→`block`，离线提示现在能显示；③ saveExtraRecord 用录入页日期而非强制今天；④ 今日概览/汇总/日报统计 `+= r.qtyIn` 补 `|| 0` 防 NaN；⑤ 今日概览 storeMap 初始化移出循环优化；⑥ 深色模式补类名浅色块覆盖（库存表头/备份步骤/别名校验/导入区/对账差异行/别名chip）。**精简冗余**：loadGoodsConfig/loadStoreConfig 抽公共 `loadNamedConfig`；saveGoodsNamesStorage 并入 saveGoodsConfig；新增 `refreshRecordViews()` 统一5处记录页刷新；renderTodayStatus 两 forEach 合并；cleanStoreName 噪音清除改一次正则。JS 语法校验通过。
- **2026-08-02（第二十九次）**：登录系统改造（commit 026025a，sw v28）。① **首页角落登录胶囊**：header 左侧，未登录"🔑登录"/已登录"👤155****8023"（中间4位星号脱敏 maskPhone）。② **账号操作菜单**：点击胶囊弹出（切换用户/退出登录/注销账号）。③ **切换用户强制密码**：走 loginModal 的 /auth/verify，绝无免密。④ **禁用免密恢复**：openVerifyModal 一律要求密码，restoreLoggedIn 不再自动触发。⑤ **账号注销**：两步确认（警告"永久删除"+输密码二次确认）+ 后悔药按钮（角落小字，点击告知数据未真删可联系管理员）。⑥ **软删除+临时代码**：注销后释放手机号、数据冻结为临时代码 tmp_<code>，管理员可恢复。⑦ **后端接口**：新增 `/auth/delete`（软注销）、`/admin/restore-account`（临时代码→新手机号）、`/admin/delete-temp`（彻底删除），admin/stats 增加 pendingDelete 列表。⑧ **管理员恢复界面**：adminModal 新增"已注销待恢复"区块，显示临时代码+原手机号，可恢复/彻底删除。⑨ **未登录锁定**：页面顶部橙色提醒横条 loginNotice + 录入/保存控件禁用（locked-input），保存函数加登录校验双保险。**功能缺入口排查**：所有功能均有入口，唯一后端 delete-account 无前端入口（已用注销+临时代码机制替代）。语法校验全过。
- **2026-08-02（第三十/三十一次）**：登录入口位置优化（commit b6ac36e + d048b1c，sw v29→v31）。① 登录胶囊从 header 移到**首页顶部**（原"数据账号"徽标位置，全宽居中按钮）；header 恢复干净不再被占用。② **更改密码**整合进登录胶囊菜单（已登录：切换用户/更改密码/退出登录/注销账号）。③ **数据备份页优化**：移除退出登录/更改密码按钮（已移胶囊菜单），accountStatus 精简为账号状态+恢复数据/登录/忘记密码+操作指引；删除"使用教程再次查看"冗余按钮；账号显示改脱敏格式。
- **2026-08-02（第三十二次）**：数据备份页删除账号卡片（commit 06563f3，sw v32），只保留"恢复云端数据"按钮。
- **2026-08-02（第三十三次）**：修复小米PWA桌面拍照/相册选不可用（commit 305b43f，sw v33）。**根因**：label 包裹 hidden input 委托 + native-file-btn 伪元素方案在小米PWA(WebView)下失效。**改法**：拍照/相册选按钮改为普通 button + onclick 动态创建**原生可见** file input（移出可视区但不隐藏）并立即 .click()，触发后移除；拍照 capture="environment"，相册不带 capture；同时适配店面拍照（triggerPhoto('camera'/'album','store')）。删除对旧 photoInput/photoAlbumInput/storeOcrAlbumInput 的引用（closeOcrResultModal 里的 value 重置）。
- **2026-08-02（第三十四次及之后）**：登录系统多轮优化 + 密码系统改造（commit 305b43f→467d704，sw v33→v40，版本 4.3.1）。包括：测试期临时取消登录锁定、版本号规则与APP_VERSION集中管理、**密码改明文存储与校验**（前端登录/改密/注销/恢复/重置全部传明文，后端verify/setup/reset/delete改明文比对并兼容旧哈希）、5200账号(13635685200)密码多次排查与重置。**注**：后端 `admin/reset` 现支持明文（newPasswordHash 可选）；`handleSetup` 不再强制 passwordHash；`handleVerify` 优先明文比对、旧哈希兜底。当前云端所有账号明文密码字段为空（旧版只存哈希），需重新设置/重置密码才会写入明文。
- **2026-08-02（第三十五次）**：密码明文**全路径生产接口实测**（commit 467d704，版本 4.3.1）。用测试号完整跑通：①设置账号 `/auth/setup` ✅ ②登录 `/auth/verify` 明文 ✅ ③改密码（verify旧密+setup新密）✅新密能登/旧密401 ④忘记密码 `/auth/reset` 密保+新密码 ✅ ⑤管理员重置 `/admin/reset` 明文 ✅ ⑥注销 `/auth/delete` 明文校验+临时代码 ✅ ⑦管理员恢复 `/admin/restore-account` 临时代码→新手机号 ✅ ⑧彻底删除 `/admin/delete-temp` ✅。测试数据已清理，不影响真实账号。
- **2026-08-02（第三十六次）**：语音识别改**云端识别（腾讯云ASR）**（沙箱侧完成，commit de6bc201，sw v41）。**根因**：小米浏览器/快捷方式下 Web Speech API（SpeechRecognition）不可用 → 语音点击无效。**改法**：前端 startVoice 改用 **MediaRecorder 录音**（所有安卓浏览器支持，优先 audio/mp4，fallback webm）+ 录音结束上传 `/tcloud-asr`（新增 Cloudflare Function，签名调腾讯云 **SentenceRecognition** 一句话识别，VoiceFormat 按 mime 映射 m4a/ogg/mp3/wav，密钥走环境变量）→ 返回文本 → 走原 parseVoiceText 确认弹窗填入。**实测**：/tcloud-asr 接口 200 签名正确（静音返回空文本）。**待办**：真机（小米）语音完整验证，若 webm 识别失败需调格式。
- **2026-08-02（第三十七次）**：拍照弹窗修复（commit 4ff0057f，sw v42）。① 拍照/相册选从 label+input 改为**按钮 + JS 动态触发**（triggerPhoto('camera'/'album')，动态创建可见 file input click），兼容小米浏览器；② **点按钮弹窗立即消失**（triggerPhoto 开头 closePhotoInputModal），再打开系统相机/相册。
- **2026-08-02（第三十八次）**：恢复"添加到桌面"功能（commit d69179dc，sw v43）。电脑端曾移除（v6.0.14 阻止原生PWA弹窗），用户要求恢复：首页"📱 添加到桌面"按钮 + installModal 引导弹窗（微信引导/苹果教程/安卓说明）+ beforeinstallprompt 保存 deferredPrompt（Chrome 直接弹"安装应用"）。manifest 加 `id` 字段增强可安装性（manifest 已完整：icons 192/512 + standalone + SW 注册）。
- **2026-08-02（第三十九次）**：PWA 安装引导方案已确认（未执行）。用户接受"安卓 APK 下载安装 + 苹果 PWA 网页版"。APK 方案：HBuilderX 云打包 或 Capacitor 命令行打包（WebView 壳加载在线网页，更新自动同步）。已给电脑端交接文档，**待电脑端执行打包**。另外已向用户说明：小米浏览器"添加到主屏幕"=网页快捷方式（无法授权权限），真 PWA 需用 Chrome 安装（应用信息→权限→允许麦克风/相机）。

- **2026-08-02（第三十六次）**：强制更新弹窗（commit fc27ba6，版本 4.3.2，sw v41）。更新弹窗改为**强制更新不可跳过**：去掉"稍后"按钮，只有"立即更新"，点遮罩不关闭，显示新版本号。目的：每次部署后强制用户加载最新版，保证顺利使用。当前 5200 账号(13635685200)密码=`Abc@123456`（明文已写入，登录验证通过）。
- **2026-08-02（第三十七次）**：**重大根因修复**（commit 1d7a01b，版本 5.0.0，sw v44）。**核心BUG**：`account_<手机号>` KV key 被 `/auth/setup`（账号认证+密码）和 `/backup`（纯业务数据，无密码字段）**共用覆盖**，导致登录后云备份把密码字段冲成空 → 改密"密码错误"、明文反复变空。**修复**：业务数据改存独立 `data_<account>` key；auth/verify、admin/restore 同步适配。**账号状态**：15558088023、13635685200 均已重置为 `12345678` 并验证登录成功。
- **2026-08-02（第三十八次）**：**全面代码审查+修复**（commit 7eb5562，版本 6.0.0，sw v46）。**修复**：① doOCR/doStoreOCR 未配密钥时全屏遮罩永久卡死（补 hideLoading）；② 注销冻结丢 uiState + data_ 缺失不回退残留业务（补全+回退）；③ migrateDataToScope 未清空全局 key 导致多账号隔离失效串号（改移动语义）；④ logout 未刷新数据视图（补 refreshRecordViews）；⑤ 切换账号云端无数据时界面不刷新（登录后无条件刷新）；⑥ kuanwei_last_save 未写入导致旧云数据覆盖新本地（保存时写时间戳）；⑦ 删除 hashPassword 死代码。**审查确认**：所有弹窗关闭路径完整、面板切换对称、前后端字段一致。**遗留安全**（测试期暂不加固，上线前需处理）：adminKey 硬编码前端、`/backup?account` 无鉴权可读、明文密码日志。
- **2026-08-02（第三十九次）**：修复语音录入动态加载效果被遮挡（commit 8f6d820，版本 6.0.1，sw v47）。**根因**：语音识别浮层 `#voiceOverlay` 与语音录入弹窗 `.store-ocr-modal` 的 z-index 同为 9998，且浮层在 DOM 中更靠前，录音时浮层被弹窗盖住，动态加载（脉冲麦克风）显示不全。**修复**：`.voice-overlay` 的 z-index 提升到 10000（最顶层），录音动态效果不被遮挡。sw v46→v47。
- **2026-08-02（第四十次）**：全面排查并修复弹窗层级遮挡同类问题（commit c199b58，版本 6.0.2，sw v48）。用子代理全量审查所有 z-index 与 DOM 顺序。**修复**：① `#loadingOverlay` 加载浮层 z-index 9998→10000（与语音浮层统一，从任何弹窗触发 OCR 都不被遮挡）；② `voiceConfirmModal`（语音识别确认）、`dailyPreviewModal`（日报预览）、`versionConfirmModal`（强制更新）三个"从父弹窗内打开/随时异步弹出"的子弹窗单独提升 z-index 至 10001，确保不被父弹窗/浮层盖住（尤其 versionConfirmModal 是强制更新不可关，若被盖会死锁）；③ `parseVoiceText` 打开语音确认框时立即收起 voiceOverlay（不再等 onend 的500ms延迟），避免浮层残留。**审查确认无需处理**：photoPanelModal 是死代码（从未调用）、OCR loading 与结果弹窗时序正确、account-menu 与 modal 同帧移除、gd-modal 删除类互斥。sw v47→v48。
- **2026-08-02（第四十一次）**：优化语音识别错误处理（commit f0a0a4d，版本 6.0.3，sw v49）。用户截图反馈小米PWA下语音录入弹"识别出错: not-allowed"。**根因**：浏览器/PWA未授权麦克风权限，SpeechRecognition.start() 拒绝启动。**修复**：① `startVoice` 启动前先 `navigator.mediaDevices.getUserMedia({audio:true})` 主动请求权限（部分PWA/WebView需此步才能弹权限框），获取后立即释放 stream（仅为触发权限）；② `onerror` 错误映射为中文友好提示（not-allowed/audio-capture/no-speech/network/aborted 等分别给出明确引导）；③ 权限类错误用醒目 toast 提示"请在浏览器/PWA设置中允许麦克风权限"；④ 错误时立即收起浮层（不等1.5s延迟），让用户可立即重试或改用手动输入；⑤ start() 用 try/catch 包裹，捕获 InvalidStateError 等同步异常。sw v48→v49。
- **2026-08-02（第四十二次）**：保存失效加固（commit f627d4d + 3d1af35，版本 6.0.4，sw v50）。用户反馈"保存按键失效"。**排查**：用 playwright 真机打开线上页验证——登录状态下保存按钮 `pointerEvents:auto`、`locked:false`，代码无 bug；保存失效最可能是"未登录导致按钮被 locked-input 锁定（pointer-events:none 点击无反应）"或"店面为空被拦截提示"。**加固**：`refreshRecordViews()` 内新增 `updateLoginLock()`，每次刷新视图强制同步登录锁定状态，根治"已登录但按钮残留锁定"的边界场景。**附带清理**：误提交的测试临时文件（.playwright-cli/、_check.js）已从 git 移除，并在 .gitignore 追加忽略规则。sw v49→v50。
- **2026-08-02（第四十三次）**：修复店面管理保存无反应（commit 9f40ee7，版本 6.0.5，sw v51）。用户明确"店面管理页面有改动但点保存没反应"。**根因**：`saveStorePanelChanges()` 内 `const cfg = loadStoreConfig()` 声明为 const，但函数底部 `cfg = cfg.map(...)`（sort 归一化）**对 const 变量重赋值** → 运行时抛 `TypeError: Assignment to constant variable`，函数在第3609行中断，导致 `saveStoreConfig`/`renderStorePanelUI`/`showToast` 都不执行 → 保存完全无反应。**修复**：`const cfg` 改为 `let cfg`。**验证**：playwright 真机在 https 域设置店面数据后调用 `saveStorePanelChanges()`，返回"调用成功，未报错"。sw v50→v51。
- **2026-08-02（第四十四次）**：修复店面排序保存丢失（commit 03c2192，版本 6.0.6，sw v52）。用户明确"排序后点保存提示成功但排序没保存"。**根因**：`saveStorePanelChanges()` 底部 `cfg = cfg.map((s, idx) => ({ ...s, sort: idx }))` **用数组物理下标覆盖 sort**；而排序机制是交换 sort 字段（数组物理顺序不变，保 storeIdx 稳定），导致用户排的序在保存时被物理下标重置丢失。**修复**：改为按现有 sort 值归一化（`sort` 排序→按显示顺序重赋 0..n-1），保留用户排序，数组物理顺序不变。**验证**：node 模拟"B移到A前→保存"，保存后显示顺序仍为 B,A，sort 正确。sw v51→v52。
- **2026-08-02（第四十五次）**：修复店面排序"刷新后又恢复"（commit 4a6ca85，版本 6.0.8，sw v54）。用户明确"保存的店面顺序又恢复了"。**根因**：`autoSyncFromCloud()`（每次刷新/登录自动调用）**无条件用云端 storeConfig 覆盖本地**；若用户保存排序后立即刷新（`scheduleCloudBackup` 有1秒防抖，云端可能还是旧排序）或备份未完成，刷新时拉到的云端旧 storeConfig 覆盖本地新排序 → 顺序恢复。**修复**：① `saveStoreConfig`/`saveGoodsConfig` 保存时写 `kuanwei_last_save` 时间戳（原仅 `saveDataArr` 写）；② `autoSyncFromCloud` 覆盖 storeConfig/goodsConfig 前做新旧判断——本地最后保存时间晚于云端 backupTime 则保留本地（不覆盖）；③ 时间戳读取移到 `saveDataArr` 之前（避免其刷新本地时间戳导致判断失效）。**注意**：此修复需用户**重新排序一次**（触发 saveStoreConfig 写新时间戳）后刷新才不恢复；若云端已是用户最新排序则无影响。sw v53→v54。
- **2026-08-02（第四十六次）**：检查并修复同类错误（commit 4b618d5，版本 6.0.9，sw v55）。用户要求"检查同类型错误并修复"，用子代理全面排查两类。**修复**：① **const 重赋值（与已修店面保存同类）**——`removeStoreFromPanel`（删除店面）`const cfg` 被 `cfg=cfg.map(...)` 重赋值 → TypeError 删除无反应；改 `let cfg` 并按 sort 语义归一化（保留剩余店相对排序）。② **云同步旧数据覆盖本地**——`autoSyncFromCloud` 原对 records 用"云端优先"合并、且无条件 `applyUiState` 覆盖本地偏好（theme/daily_pref/ocr_keys 等）；改为整体套 `cloudNewer` 判断——仅云端比本地新才合并 records/覆盖配置/偏好，本地新则全部保留，彻底杜绝"刷新后数据回退"。**审查确认无需处理**：`doVerifyAccount`/`restoreFromCloud` 是用户主动"恢复数据"语义（以云端为准，合理）、`restoreLoggedIn` 是 dead code（无调用）、其余 40+ 处 `.map/.filter` 重赋值均为 `let` 安全。sw v54→v55。
- **2026-08-02（第四十七次）**：修复 PWA 桌面快捷方式拍照/相册选无效（commit 20cf39b，版本 6.0.10，sw v56）。用户明确"手机桌面快捷方式进入拍照相册选无效，是不是权限问题"。**根因**：`triggerPhoto()` 动态创建的 file input 用 `left:-9999px` **移出可视区**——PWA/standalone WebView 下浏览器因 input 不可见而**阻止唤起系统文件选择器**（拍照/相册都无效）；且拍照需相机权限。**修复**：① `triggerPhoto` 的 input 从"移出可视区"改为**全屏透明可见覆盖**（`position:fixed;top:0;left:0;width/height:100%;opacity:0`，与已验证可靠的 `triggerAlbumInput` 一致）；② 拍照前主动 `navigator.mediaDevices.getUserMedia({video})` 请求相机权限（获取后立即释放）。**结论**：既有权限因素（相机需授权），也有实现因素（input 不可见被阻止唤起）；两者均已处理。所有触发点（拍照识别弹窗 2015/2016、店面拍照 1168/1169）统一走 `triggerPhoto`，已全部覆盖。sw v55→v56。
- **2026-08-02（第四十八次）**：安卓专属拍照/相册选失效修复（commit b7b9a46，版本 6.0.11，sw v57）。用户补充"只有安卓系统出现，苹果正常"，并发视频演示。**根因**：安卓 Chrome/WebView 对**JS 程序调用 `input.click()` 触发 file input** 有严格安全限制（file chooser 必须在真实用户激活手势中打开），动态创建 input + 程序 click 在安卓被拦截，故拍照/相册都无反应；iOS 对程序触发更宽松所以正常。**修复**：不再用 JS 触发，改为 **`<label>` 包裹原生可见 file input**（input 用 `position:absolute;opacity:0` 覆盖整个 label 区域），用户直接点击 label/input 即真实用户激活，安卓必响应。四处按钮（拍照识别弹窗 拍照/相册、店面拍照 拍照/相册）全部改为 label 方案，capture 属性区分拍照/相册。**附带**：静态 input 需在 `handlePhoto` 里重置 value（选同一文件可再次触发 onchange）。**注意**：用户视频是修复前录制（v6.0.10 之前），需刷新到 v6.0.11 后重测。sw v56→v57。
- **2026-08-02（第四十九次）**：移除"添加到主屏幕"功能（commit fb7def1，版本 6.0.12，sw v58）。用户明确"移除添加到主页的功能"。**移除内容**：① 首页「📱 添加到主屏幕」按钮（1058）；② 引导弹窗 `installModal` 及其内 3 个提示容器（wechat/normal/android）；③ PWA JS——`beforeinstallprompt` 监听、`deferredPrompt` 变量、`installApp()`、`closeInstallModal()`；④ 使用说明里的「第1步：加到手机桌面」整个教程卡片 + 首段"建议先做三件事"里的"添加到主屏幕"条目，并将剩余步骤重编号（原第2/3/4/5步 → 第1/2/3/4步，首段"三件事"改"两件事"）。**验证**：全文件搜索 installApp/installModal/添加到主屏幕 等关键词 0 残留。sw v57→v58。
- **2026-08-02（第五十次）**：更新使用说明（commit 944e3ef，版本 6.0.13，sw v59）。用户明确"更新使用说明"。**修正**：上一轮移除功能时部分步骤标题编号遗漏未改，本次修正为——「📝 第4步：每天怎么录入数据」→ **第3步**、「🔐 第6步：数据安全」→ **第5步**。修正后完整编号：第0步认识页面、第1步设置店面、第2步设置账号密码、第3步录入数据（手动/语音/拍照3种方法）、第4步查看核对数据、第5步数据安全、常见问题。**核验**：使用说明其余内容（手动/语音/拍照录入路径、账号密码、恢复/忘记密码、常见问题）与当前功能一致，无过时项。sw v58→v59。
- **2026-08-02（第五十一次）**：阻止浏览器原生 PWA 安装弹窗（commit 761aa58，版本 6.0.14，sw v60）。用户截图反馈安卓系统仍弹出"是否将该网站添加到桌面以便离线查看"原生提示。**根因**：上轮移除了应用内"添加到主屏幕"按钮和引导弹窗，但**安卓 WebView 自身仍会在 PWA 条件满足时弹原生安装提示**（beforeinstallprompt 由浏览器触发，应用代码无法完全删除）。**修复**：注册 `beforeinstallprompt` 监听并 `e.preventDefault()`，静默阻止系统级弹窗（不暴露任何应用内入口）。sw v59→v60。

- **2026-08-03（第五十二次）**：Capacitor APK 打包 + 安卓拍照/云同步问题修复（commit 863b5bb + 90c33f1 + 7e4090a，版本 6.0.15，sw v61）。用户改用 Capacitor 方案打安卓APK（绕开 DCloud wap2app 域名备案问题）。**APK 打包**：Capacitor 项目 `com.kuanwei.inventory`，server.url 加载线上 https://inventory-app-9ql.pages.dev/，`www/index.html` 壳，AndroidManifest 加权限（相机/录音/定位/网络/存储），图标用 apk_icons（adaptive icon foreground/background），启动页用用户 splash_1080x1882.png。**构建环境**：本机 Java 25 需 Gradle 9（Gradle 8 不支持）；路径含中文需 `android.overridePathCheck=true`；AGP 8.13 兼容 Gradle 9；Gradle 从腾讯镜像下载。**修复用户反馈**：① 拍照/相册"点返回后点任何地方唤起相机/文件管理"——根因是 `triggerPhoto`/`triggerAlbumInput` 用**全屏透明 fixed input**，用户取消选择时 onchange 不触发、input 残留导致全屏点击劫持；改为**按钮 label 方案**（input 只覆盖按钮区域）+ `oncancel`/`onblur` 立即清理。② 云端数据同步不过来——根因是 Capacitor WebView 下**相对路径 fetch('/backup') 被拦截**；把所有 fetch 改为**绝对地址** `API_BASE + '/xxx'`（`API_BASE='https://inventory-app-9ql.pages.dev'`）。③ 图标没用准备好的——adaptive icon 需正确配置 foreground/background，已按 apk_icons/README 放入。**APK 产物**：`物流筐-debug测试版2.apk`（5.3MB，debug签名）。**备注**：node_modules/android/build 已加入 .gitignore，构建产物不入库；本地 .bak 备份移到 backup_icons/ 目录（不参与 Android 构建）。

## 一、核心结论（当前状态）

**项目已完整交付，可正常使用。** 从 v1 迭代到最新版，全功能上线：

| 项 | 状态 |
|----|------|
| 程序 | ✅ 完整可用（单 HTML 文件 3840 行） |
| 部署 | ✅ Cloudflare Pages 固定网址（不再用临时隧道） |
| 云端 | ✅ KV 自动备份 + 手机号密码账号体系 + 管理员重置 |
| 产物 | ✅ 演示视频（67秒 mp4）+ 功能演示 PPT（11页） |
| 质检 | ✅ 最高规格 19 项全过（语法/结构/安全/接口全通） |

## 二、资源位置速查

| 资源 | 位置/链接 |
|------|-----------|
| **线上网址（永久固定）** | https://inventory-app-9ql.pages.dev/ |
| **代码仓库（GitHub）** | https://github.com/xiaokai8237-boop/inventory-app |
| **演示视频下载** | https://inventory-app-9ql.pages.dev/demo.mp4 |
| **演示PPT下载** | https://inventory-app-9ql.pages.dev/demo.pptx |
| **主程序** | `/workspace/index.html` |
| **云端接口** | `/workspace/repo/functions/`（backup.js / proxy.js / auth/[[path]].js / admin/[[path]].js） |
| **演示单据图** | `/workspace/演示单据.png` |
| **录屏脚本** | `/workspace/demo_record.py` |
| **PPT生成脚本** | `/workspace/gen_ppt.js` |
| **本地代理脚本** | `/workspace/proxy_server.py`（如需本地跑OCR） |

## 三、账号体系（重要，勿泄露）

| 项 | 值 |
|----|-----|
| 管理员密钥 | `8023.520` |
| 管理员入口 | 使用说明页最后一条"每天要录很多店怎么办？"前 ❓ 连点 5 次 → 输入管理员密码（8023.520）→ 进入面板 |
| 临时重置密码 | `12345678` |
| 密码规则 | ≥8位，含字母+数字+符号 |
| 手机号规则 | 11 位，1 开头 |
| 安全机制 | 云端只存哈希、输错 5 次锁 10 分钟、密保问题找回 |

## 四、功能清单（当前版）

- ✅ 店面管理（增删改+别名+拍照批量导入+保存按钮）
- ✅ 5种筐类型管理（增删改+别名）
- ✅ 录入三方式：手动 / 语音（Web Speech）/ 拍照OCR（百度）
- ✅ 拍照识别：路单自动识别物流箱，可编辑/新增行/列偏移
- ✅ 历史记录（筛选/删除/来源标记）
- ✅ 汇总（日期+店面维度，单店汇总）
- ✅ 对账（导出Excel/导入对比差异）
- ✅ 库存追踪（发出/回收/待收）
- ✅ 数据备份与恢复（导出/导入 .json）
- ✅ 云备份（Cloudflare KV 自动备份）
- ✅ 账号密码体系（设置/恢复/改密/忘记密码/密保找回）
- ✅ 管理员重置（隐藏入口）
- ✅ 傻瓜教程（保姆级使用说明 + 备份教程弹窗）
- ✅ 加载动画/自适应/emoji/新UI

## 五、电脑上如何继续开发

```bash
# 1. 克隆仓库
git clone https://github.com/xiaokai8237-boop/inventory-app.git

# 2. 改完推送（Cloudflare Pages 自动部署，1-2分钟生效）
git add -A && git commit -m "说明" && git push

# 3. 本地预览（纯界面，OCR需云端）
python3 -m http.server 8080  # 打开 http://localhost:8080/
```

> 部署说明：Cloudflare Pages 项目已绑定 KV（变量名 `BACKUP_KV`），推送即自动部署，无需手动。测试账号数据已清理/测试用。

## 六、遗留决策口子（你拍板）

1. **做不做模板化改造**（通用化后卖价翻倍，受众从"物流配送"扩到"任意进出库管理"）？
2. **卖源码 还是 SaaS 化**？决定下一步开发方向。

## 七、注意事项

- 数据每台手机独立（localStorage）+ 云端账号备份（手机号+密码）
- 清缓存后：输入手机号+密码从云端恢复
- 云端接口免费额度：Cloudflare Pages 免费、KV 免费（写入10万/天）、百度OCR默认密钥已预置（可能限流，卖出去需换独立key）
- 管理员入口是隐藏的，勿写进公开教程
